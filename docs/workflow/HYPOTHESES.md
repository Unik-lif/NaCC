# Hypotheses

Keep only hypotheses that are still worth validating. Rejected hypotheses should remain here too, with their counter-evidence.

| Hypothesis | Supporting Evidence | Contradicting Evidence | Confidence | Next Validation |
| --- | --- | --- | --- | --- |
| The current standard fork child secure-PTP path depends on Linux-native `dup_mmap()/copy_page_range()` plus secure hooks, not legacy `nacc_fork` bypass | Code inspection shows `dup_mmap()` still goes through `copy_page_range()` and NaCC hooks in `__pte_alloc/__pmd_alloc` and `set_pte/set_ptes`; `nacc_fork/sm_nacc_fork` are treated as legacy | Runtime logs on the newest baseline have not yet re-confirmed the full child secure page-table build path | high | Re-run fork smoke and observe `__pte_alloc/__pmd_alloc` and `nacc_set_ptes_sbi` logs |
| The current `NACC_FORKED` child is more likely missing first-user-return attach / runtime completion than a full extra `transfer_ptp` pass | The code now has parent early child registration and first-user-return attach, which matches the "secure PTP grows gradually through the native path" model | There is still no new runtime result proving that attach is enough for pipelines or non-exec child cases | medium | Re-run `echo alpha | cat`, `echo alpha | wc -c`, and builtin-heavy shell tests |
| The `NACC_EXEC` transition state allows the fresh exec `mm` to be built first on the ordinary Linux page-table path, then secured through `nacc_exec()/sm_nacc_exec()` | `INITED -> NACC_EXEC` now happens before `bprm_mm_init()`; failures before point-of-no-return restore `INITED`; same-pid exec and fork+exec now share one exec-attach path | There is still no runtime proof on the latest baseline that the fresh `mm` never enters secure-page-table hooks too early | medium | Re-run minimal same-pid exec and fork+exec on the latest baseline and inspect build / attach logs |
| Simple fork smoke may have crossed the minimum barrier, but higher-level container semantics may still re-expose the old accounting / reclaim issues | A user report on 2026-03-22 claimed `cat /etc/hostname; echo done` looked passable, suggesting some fork baseline progress | That result was never fully grounded with checkpoints and logs, and shared-memory / `mmap` / stress coverage was not completed | medium | Execute Tier 0 to Tier 2 and watch for accounting regressions |
| `echo alpha | wc -c` still looks more like a file-backed page-fault / `filemap_map_pages()` path issue than a `wc`-specific application bug, but this must be revalidated after the latest fork/exec patches | Older logs showed page faults after `sys_pipe2`, then `Kernel filemap_map_pages`, then `SEGV_ACCERR`, while VM-side business output was missing | The latest code significantly changed `NACC_FORKED` attach and exec-build state, so old logs are no longer enough to define the current root cause by themselves | medium | Re-run `echo alpha | cat`, `wc -c /etc/hostname`, `busybox wc -c /etc/hostname`, and `cat /etc/hostname` |
| Shared memory / `mmap` is likely the next place where semantic gaps will surface | The paper motivation explicitly includes multi-process shared memory, and real runtimes rely heavily on `mmap`, `MAP_SHARED`, file mappings, and loader semantics | There is not yet fresh negative evidence proving these cases fail on the latest baseline | medium | Execute Tier 2 from `PLAN_20260322_container_validation.md` |
| Earlier `rss` / `rmap` / `pgtables_bytes` risks may still exist, but simply have not reappeared in the current small smoke cases | The 2026-03-17 log clearly showed `Bad rss-counter state` and `non-zero pgtables_bytes`; current evidence only adds lighter positive signals | If Tier 0 to Tier 2 stay stable over repeated runs, this hypothesis should quickly be downgraded | medium | If the issue reappears, fall back to `TICKET_20260317_fork_accounting_observability.md` |
| The current hard-to-explain loader / post-fork progression failures may come from incomplete trusted runtime-context modeling rather than a single filemap or single-PTE defect | Multiple recent symptoms now point toward failure to converge execution progress rather than failure to install one faulted PTE; the runtime-state model currently looks too hart-local | No direct implementation of `nacc_thread_ctx` exists yet, so this is still a design-level hypothesis rather than a proven root cause | medium | Inventory runtime state, classify per-hart vs per-thread ownership, and test a proper context save/load design |
