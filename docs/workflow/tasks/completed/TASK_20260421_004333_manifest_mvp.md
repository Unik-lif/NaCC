# Task Packet

- Task ID: TASK_20260421_004333_manifest_mvp
- Created: 2026-04-21 00:43:33 +0800
- Priority: P0
- Lane: A
- Packet Type: execution
- Owner Role: human
- Status: blocked
- Goal: Deliver Phase 2 minimal manifest-driven startup sealing incrementally so startup protection policy at the post-link / pre-user-entry boundary is derived from a manifest (objects + PT_LOAD segments), not from Linux VMA annotations.
- Critical Intent: Linux may provide runtime load bases or mapping facts only as coordinates, never as security authority. Phase 2 must be split into small PRs, and the coder should commit after each PR is considered handled. PR4 remains manifest-audit only: for the current PR1 object set (`entry` and optional `interp`), it must turn manifest-derived object/segment facts plus PR3 runtime coordinates into a startup-only audit loop before first user entry, without changing leaf-tag decisions or startup sealing behavior. At the current checkpoint, the accepted PR4 audit code is not being reopened; the only unresolved route choice is whether to park PR4 with matched validation still missing, or to approve exactly one more validation-only closure-staging slice that keeps the audited startup object set fixed as `entry` and optional `interp`.
- Preferred Shape: Land Phase 2 as six small PRs with clean review boundaries and a coder commit after each PR is considered handled. Keep the startup authority split explicit: manifest-derived object/segment facts decide startup sealing intent; Linux runtime reports provide load bases or mapping coordinates only. For PR4 specifically, prefer the smallest startup-only audit slice that reads the already PR2-installed guest manifest artifact, reconstructs expected runtime ranges for the current PR1 object set (`entry` and optional `interp`) from manifest `PT_LOAD` data plus PR3 raw runtime bases, and emits audit-only evidence against the existing startup/root-cid state. Treat this as an MVP flow-bring-up step, not the final architecture for manifest delivery. For the current validation checkpoint, the pair-only disposable probe-root route is now exhausted: it preserves the intended kernel `PT_INTERP` proof model and no longer crashes guest PID 1, but it still does not reach any startup coord/audit evidence. The preferred continuation is conditional rather than silent: if the human approves one more PR4 validation slice, keep it top-level and validation-only, derive the exact transitive runtime closure for the fixed manifest `entry` under the exact manifest-resolved interpreter/toolchain/sysroot, stage it only into the disposable probe root, and rerun the positive matched case before any mismatch-table repetition. Do not reopen Linux/OpenSBI audit semantics in that slice unless the first new artifact proves host-side closure staging alone still cannot exercise the accepted PR4 code path. If the human does not approve that slice, explicitly park PR4 as `code accepted, matched end-to-end validation still unresolved on the fixed workload` rather than smearing the gap into PR5.
- Disallowed Shape: Do not collapse Phase 2 into one large cross-subsystem landing. Do not keep Linux VMA metadata on the startup security decision path. Do not use manifest information to create de-protection or shared exceptions in this phase. Do not widen scope into syscall-buffer performance tuning, shared apertures, attestation/signing, or a broader runtime-policy redesign. For PR4 specifically, do not change `nacc_region_select_leaf_tag`, do not alter current `PRIVATE_DATA` startup behavior, do not widen into broader runtime-DSO or post-startup mmap coverage as production semantics, do not introduce SCP as part of the MVP route, and do not pretend this PR settles the long-term registration-path architecture. The current validation follow-up must not keep using guest-global `/lib/ld-linux-riscv64-lp64d.so.1` replacement as the matched-proof route now that runner-owned evidence shows it crashes PID 1 before any audit evidence appears. Do not redefine the manifest/workload pair, do not silently treat the guest's existing `/lib/ld-linux-riscv64-lp64d.so.1` or guest-native DSOs as equivalent to exact staged proof artifacts, do not treat direct manual `ld-linux ... entry` invocation as an equivalent proof because it bypasses the kernel `PT_INTERP` exec path, do not reinterpret any extra staged runtime DSOs as newly audited manifest objects, do not treat the passing ordinary baseline smoke as matched PR4 proof, do not reopen Linux/OpenSBI/agent in the next slice unless the first closure-staged artifact points there, and do not widen to a broader staged runtime bundle or host-sysroot refresh without an explicit human-approved planner checkpoint.
- Allowed Freedom: Coder may choose the exact Linux helper boundaries, Linux<->OpenSBI wire format, temporary in-guest manifest-consumer path, and OpenSBI audit log detail as long as PR4 stays startup-only and audit-only. Linux may temporarily read or marshal the already installed manifest artifact for this MVP step, but audit verdicts and expected-range comparison should remain monitor-owned. The first PR4 proof may study the ELF corresponding to the currently installed guest manifest artifact rather than introducing a new SCP-driven test path. If the human explicitly approves a wider validation slice, coder may either extend `scripts/install_manifest.py` narrowly or add sibling top-level helper(s), as long as the change stays validation-only, stages a disposable guest-local probe root, keeps the guest-global interpreter untouched, and sources any extra staged runtime DSOs from the exact runtime closure of the fixed manifest `entry` under the same resolved toolchain/sysroot rather than from arbitrary guest files. That closure may be derived by `readelf`, linker trace, or equivalent host-side introspection; the mechanism is free, but the staged files must be exact bytes from that same closure and should be preserved as a runner/reviewer-readable artifact list. Keep Linux/OpenSBI unchanged in that slice unless the first new artifact proves the accepted PR4 path is still not being exercised at all. Test runner may still use a one-off manual QEMU launch that preserves the existing `nacc-qemu` / `nacc-vm` pane titles and logger-compatible log capture while adding `nacc.manifest_mode=audit` to the kernel append string, because the checked-in `make launch` / `make debug` path still hardcodes `-append` with no extra cmdline override. The guest-side launch primitive may be `chroot` or an equivalent root-relative exec path, provided the kernel still resolves `PT_INTERP` against the exact staged interpreter inside the probe root.
- Scope: Phase 2A/2B MVP only. Include PR0-PR5 as separate incremental landings: logging scaffold, host-side manifest.json generation, guest delivery of manifest.json, runtime load-base report, manifest audit, and monotonic manifest enforcement. Phase 2C items remain future hooks only.
- Constraints: Do not infer security authority from Linux VMA metadata for startup protection decisions in the target end state. Do not expand scope into syscall-buffer performance work, shared exceptions, de-protection paths, image signature/attestation, or Phase 3 shared aperture work. Manifest use in this phase must be monotonic: it may tighten or audit, but must not relax protection. Unknown ordinary user leaves remain private by default. Keep packet structure intact and deliver as several PRs rather than one large change.
- Open Semantic Questions: Whether current PR1 manifest content is semantically rich enough to replace Linux VMA-derived startup classification for private-versus-shared intent, or whether additional manifest semantics must be added before PR4/PR5 can do more than range reconstruction; whether PR5 uses conservative `PRIVATE_DATA` for `MEASURED_RO` or a real RO-protected path if already available; whether the human approves the planner-recommended one-more validation-only slice (`PR4C4 closure-staged matched rerun`) that stages the exact transitive runtime closure needed by the fixed manifest `entry` while keeping the audited startup object set fixed as `entry` and `interp`, or instead parks PR4 here with matched validation still unresolved; if that wider validation slice is approved, whether its host-side implementation should auto-resolve the closure or may accept an explicitly enumerated exact file list, provided both routes stay inside the same manifest-resolved host/toolchain closure with no guest-native substitution; whether the current host-generated proof target (`/tmp/nacc_manifest_smoke_20260421_111813.elf` plus host/toolchain-resolved closure) is itself semantically misaligned enough that the next validation route should pivot toward a guest-resident workload / guest-rootfs-derived artifact instead of further widening host-side closure staging; or whether the real requirement is narrower: keep host-side generation/tooling allowed, but force the proof target and closure source to align with the RISC-V guest runtime environment rather than treating the x86 host/generator location as the problem by itself.
- Human Concern: Phase 1 strict-private startup exposed heavy syscall-buffer / usercopy trap cost, but Phase 2 should first remove Linux VMA metadata from the startup trust decision rather than optimize those hot paths prematurely.
- Key Assumptions: Phase 1 strict private baseline is already accepted. The immediate objective is startup authority cleanup at the post-link / pre-user-entry boundary, not runtime sharing or performance recovery. The user wants a sequence of small PRs, and each handled PR should end with a coder commit before the packet moves on. For PR0 specifically, Linux-side cmdline plumbing plus startup-path logs is sufficient; monitor-side consumption is intentionally deferred because this slice must stay logging-only and preserve the current sealing path unchanged. For PR1 specifically, the minimal generator slice may stop at the entry ELF plus a PT_INTERP-resolved interpreter object, emit `manifest.json` only, and defer compact `manifest.bin` plus broader runtime DSO discovery to later PRs. If PT_INTERP is present but cannot be resolved under the explicit search roots supplied to the generator, including the zero-`--search-root` case for an absolute interpreter path, PR1 treats that as a hard tool error instead of inventing a placeholder manifest object. For PR2 specifically, the smallest safe guest-delivery slice is pre-boot only: take a caller-supplied `manifest.json` unchanged, install it into `NaCC.qcow2` at `/etc/nacc/manifest.json`, and defer any consumer, schema translation, or runtime path discovery to PR3+. The repo's existing `qemu-nbd` + mounted-`rootfs` workflow is the intended delivery mechanism for PR2, and it must not be used while QEMU is running. The PR2 helper may rely on the repo's existing non-interactive root helper convention via `ROOT_SUDO` (default `sudo -n`) plus the repo-owned `/dev/nbd0` + `rootfs` staging slots rather than inventing a new privilege or mount path. For the PR2 SSH readback follow-up, the least invasive harness-safe route is additive: preserve the current banner wait, retry loop, and auto-run SSH path, but require one authenticated `ssh ... true` round trip before declaring the session ready and bound auto-run hangs with explicit timeout logging; `VM_SSH_READY_TIMEOUT_SECONDS` and `VM_SSH_AUTO_TIMEOUT_SECONDS` exist only to relax those new bounds for longer existing flows without changing the default control model. For PR3 specifically, the narrowest route is to capture raw runtime coordinates in the ELF exec path, cache them on the new protected mm, and report them through the existing NaCC startup transition path (`invoke` / `exec` and any exec-built child-reattach path) into OpenSBI-owned state without consuming the manifest yet. The preserved PR2-installed manifest artifact is a transport proof only and does not need to be workload-matched for PR3; the first workload-matched manifest choice is deferred to PR4/PR5. For PR4 specifically, the long-term preferred architecture is to ingest manifest information near container/CID registration, where OpenSBI already receives container registration state, and a later step may involve runtime code such as `runC` reading Linux filesystem content and passing manifest state along that registration path. PR4 itself is intentionally smaller than that long-term route: it may temporarily consume the already installed guest manifest artifact at `/etc/nacc/manifest.json` so the startup-only manifest audit loop can be brought up first. The first PR4 proof should study the ELF corresponding to that already-installed guest manifest artifact, while more convenient test-delivery paths such as SCP can wait for later rounds. For PR4 mode semantics, `nacc.manifest_mode=enforce` should behave as audit-only with an explicit “no enforcement yet” note, and any real enforcement remains deferred to PR5. The old validation assumption that the exact pair could be proven by replacing the guest-global `/lib/ld-linux-riscv64-lp64d.so.1` is now disproved by runner-owned evidence: that route crashes PID 1 before any PR4 audit logs appear. The pair-only disposable probe-root assumption is now also disproved as a sufficient positive proof route: it preserves guest boot and reaches the `chroot` auto-start boundary, but still produces no coord/audit evidence. The passing plain baseline smoke on 2026-04-23 is sufficient evidence that the ordinary container path still works with `manifest_mode=off`, so the current blocker is treated as PR4-validation-path-specific rather than a general runtime regression. The planner recommendation is therefore one more top-level validation-only slice before parking PR4, but that remains a human approval gate rather than an automatic machine-to-machine continuation. If that slice is approved, the first attempt should keep Linux/OpenSBI unchanged and should widen only the disposable probe root to the exact transitive runtime closure of the fixed workload, with positive matched evidence required before any mismatch-table rerun resumes.
- Context Hygiene: downstream roles should read the packet top matter, `Required Artifacts`, the PR4 active-slice summary, and `Next Handoff` first. Treat this packet as the source of truth for the current Phase 2 / PR4 state: `docs/workflow/CURRENT_STATE.md` and `docs/workflow/NEXT_STEPS.md` still provide background, but they lag the manifest PR progression reflected here. Do not reload the full PR0-PR3 history or large old logs unless the current PR4 step is blocked, because that context volume adds pressure without improving execution quality.
- PR3 debug harness assumption: the least invasive follow-up for the pre-auto-run runner failure is to treat it as a paused-debug control-path race, not a new Linux/OpenSBI runtime regression. Evidence is that the runner-owned QEMU log never moved past `[NaCC][qemu-run-start]`, the paired VM pane stayed in the `Waiting for VM (localhost:2222) to operate...` loop, and earlier passing runner flows for the same `make debug` path explicitly recorded a manual `gdb continue` step before boot progressed.
- PR3 detached rerun cleanliness assumption: reviewer/test_runner should ensure no prior repo-owned `qemu-system-riscv64` instance still holds `NaCC.qcow2` or the forwarded debug/SSH ports before the next fresh detached `make debug` rerun. This coder follow-up makes stale-owner collisions explicit and keeps the `nacc-qemu` pane visible on failure, but it does not auto-kill an existing guest.
- PR3 pager-state assumption: for this host-harness-only follow-up, the current tmux pane screen is the authoritative source for active GDB interaction state; full pane history may retain stale pager text after GDB resumes, so pager matching must ignore scrollback and tolerate prompt line-wrapping rather than rely on an exact single-line grep of the full history.
- Evidence / Inference Boundary: The PR0-PR5 route, non-goals, and success criteria in this packet come directly from the human seed in this session, and the human has now explicitly approved treating the runner-owned PR3 evidence as sufficient to move on while also requiring a discussion-first PR4 checkpoint before any coder handoff. Evidence from repository state: `Makefile` already mutates `NaCC.qcow2` through `qemu-nbd` + mounted `rootfs`; `Makefile:237` already uses `chroot` operationally on a mounted guest root; `docs/agent/DISK_REPAIR_20260316.md` explicitly warns not to run those write paths while QEMU is still running; `scripts/generate_manifest.py` already emits manifest `objects[]` with role-tagged `PT_LOAD` segment facts for `entry` and optional `interp`; PR2 already proved guest delivery of `/etc/nacc/manifest.json`; PR3 already stores startup runtime bases in OpenSBI root state keyed by root/cid; and the existing OpenSBI region database already supports active range lookup and dump. Additional runner-owned evidence in the packet: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_stage_probe_20260422_215220.log` shows the guest `/lib/ld-linux-riscv64-lp64d.so.1` hash does not match the manifest-resolved interpreter artifact; `/tmp/nacc_manifest_smoke_20260421_111813.c` contains `int main(void) { return 0; }`; `readelf -d/-V` on `/tmp/nacc_manifest_smoke_20260421_111813.elf` shows only `NEEDED libc.so.6` plus `GLIBC_2.34`; `readelf -V` on `/home/link/NaCC/riscv-tools/sysroot/lib/ld-linux-riscv64-lp64d.so.1` shows `GLIBC_2.27`, `GLIBC_2.34`, `GLIBC_2.35`, and `GLIBC_PRIVATE`; `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_stage_exact_20260422_230329.log` proves the exact entry/interpreter bytes were staged successfully into the guest; `logs/TASK_20260421_004333_manifest_mvp_pr4_positive_qemu_20260422_231111.log` proves that replacing the guest-global interpreter leads to `init[1]` crashing in `libc.so.6` before any `manifest startup coord` or `manifest startup audit` lines appear; the newer runner-owned probe-root artifacts (`logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_stage_probe_20260423_000122.log`, `logs/TASK_20260421_004333_manifest_mvp_pr4_probe_positive_20260423_000122_vm_20260423_001011.log`, `logs/TASK_20260421_004333_manifest_mvp_pr4_probe_positive_20260423_000122_qemu_20260423_001011.log`, plus the matching mismatch-run logs) prove that the pair-only probe root preserves guest boot and reaches the `chroot` auto-start boundary, yet still yields zero startup coord/audit markers; and the standard-path baseline artifacts `logs/test_runner/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933.log`, `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_vm_20260423_011256.log`, and `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_qemu_20260423_011256.log` prove that the ordinary container path still reaches authenticated SSH, prints guest-visible `test`, and emits startup-coordinate lines with zero manifest-audit activity. Human-provided architecture direction in this session is that long-term manifest ingestion should happen near the container/CID registration path rather than by treating direct startup-path file parsing as the final design. Planner inference for the next turn is narrower: the invalidated parts are the guest-global replacement proof route and the pair-only probe-root sufficiency assumption, not the accepted PR4 audit code or the general container baseline. The planner recommendation for one more validation-only closure-staging slice is an inference from those saved artifacts plus `readelf` output; no runner-owned artifact yet proves that staging the exact transitive runtime closure will succeed, and the specific guess that `libc.so.6` is the first missing object remains inference rather than proof. Likewise, the newer human concern about the current proof target is currently a planning inference about semantic alignment to the RISC-V guest/runtime environment, not proof that x86-host-side generation or cross-building is intrinsically invalid for this project. The saved artifacts only support the narrower claim that the current proof target may be misaligned with the runtime reality we want to validate.
- Reconciliation Required: no
- Post-Run Analysis Required: no
- Human Checkpoint Required: yes
- Definition Of Done: The task is complete when the work is planned as separate PR-sized increments covering PR0-PR5, with each PR preserving the stated non-goals and monotonicity rules, and with explicit expectation that the coder commits the changes once each PR is handled. Success criteria across the phase are: in audit mode, an end-to-end manifest pipeline produces expected startup segment ranges aligned with runtime mappings without enforcement; in enforce mode, startup protection decisions no longer depend on Linux VMA metadata, PRIVATE_INIT is sealed as PRIVATE_DATA, MEASURED_RO is conservatively protected in MVP, unknown ordinary user leaves remain private by default, and the system still boots/runs.
- Related State:
  - `docs/workflow/CURRENT_STATE.md`
  - `docs/workflow/HYPOTHESES.md`
  - `docs/workflow/NEXT_STEPS.md`
- Related Ticket / Plan:
  - `docs/workflow/tasks/completed/TASK_20260419_115218_private_baseline.md`
- Branch / Worktree:
- Validation Tier: T1

## Reference Values

- Priority: `P0` / `P1` / `P2` / `P3`
- Lane: `A` / `B` / `C`
- Packet Type: `execution` / `planning` / `analysis`
- Owner Role: `human` / `planner` / `coder` / `reviewer` / `test_runner` / `log_analyzer`
- Status: `draft` / `in_progress` / `needs_review` / `changes_requested` / `needs_test` / `needs_analysis` / `test_failed` / `blocked` / `done`
- Validation Tier: `T0` / `T1` / `T2` / `T3`
- Reconciliation Required: `yes` / `no`
- Post-Run Analysis Required: `yes` / `no`

## Required Artifacts

- Patch or commit: none for the immediate human checkpoint. If the human approves continuation, the next coder slice should land as one top-level commit only; do not reopen `linux/`, `opensbi/`, or `agent/` in that slice unless the first new artifact proves host-side closure staging alone still cannot exercise the accepted PR4 audit path.
- Minimal compile result: none for the immediate human checkpoint. If continuation is approved, require `python3 -m py_compile` for the touched top-level helper(s) plus `git diff --check`; reuse the already accepted `make opensbi` / `make linux-update` results unless the coder actually changes those trees.
- Closure artifact list: if continuation is approved, preserve a runner/reviewer-readable exact file list for every extra staged closure object, including host source path, guest probe-root destination path, and content hash; do not treat unstated guest-native files as part of the matched proof.
- Test command or batch plan: none before the human decision. If continuation is approved, the first runner-owned rerun must stage the exact transitive runtime closure of the fixed manifest `entry` under the same manifest-resolved interpreter/toolchain/sysroot into the disposable probe root, run the same `chroot /tmp/nacc_manifest_probe_root /tmp/nacc_manifest_smoke_20260421_111813.elf` path under `nacc.manifest_mode=audit`, and stop at the first decisive artifact. Accept only startup coord/audit evidence as a positive proof. Re-arm the mismatch-table rerun only after a positive matched pass emits those markers. If the first closure-staged rerun still fails before audit evidence, stop and return to planner/human without widening scope again.
- Primary log path: if continuation is approved, `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_closure_positive_<timestamp>.log`
- Log path if validation fails: preserve the first closure-staged failing VM/QEMU artifacts and any host-side closure artifact list; do not continue to mismatch after a failing positive pass

## Latest Summary

- Phase 2 remains the active execution round: startup authority must move from Linux VMA annotations to manifest-derived object/segment facts at the post-link / pre-user-entry boundary, and the work should continue as narrow PR-sized slices rather than one large landing.
- PR0 is closed:
  - Linux commit `ee3b3c8504da60c1f2608f59a8e6e4fc684784cc` added `nacc.manifest_mode={off,audit,enforce}` parsing plus startup-only scaffold logs with no intended behavior change.
  - reviewer approved the PR0 route and test_runner recorded a passing single-object compile proof in `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr0_t1_20260421_005748.log`.
- PR1 is closed:
  - implement a host-side minimal manifest generator for the entry ELF plus a PT_INTERP-resolved interpreter object
  - emit `manifest.json` only in this slice
  - defer compact `manifest.bin`, guest delivery, runtime load-base reporting, audit logic, and enforcement to later PRs
- PR1 coder implementation is now ready for review:
  - top-level commit `7aeca8ef5ee749e32872d24e57405d9ecb3bc90e` (`[CODE]: manifest add minimal host generator`) adds `scripts/generate_manifest.py`, a standalone host-side ELF parser/generator for the entry ELF plus one PT_INTERP-resolved interpreter object
  - bounded local proof emitted `manifest.json` for a throwaway RISC-V dynamic ELF and resolved `/lib/ld-linux-riscv64-lp64d.so.1` under `riscv-tools/sysroot`; summary log: `logs/coder/TASK_20260421_004333_manifest_mvp_pr1_manifest_smoke_20260421_012505.log`
- Reviewer found one PR1 fidelity blocker in commit `7aeca8ef5ee749e32872d24e57405d9ecb3bc90e`:
  - `scripts/generate_manifest.py` still resolves absolute `PT_INTERP` via the host root filesystem when no `--search-root` is supplied, so PR1 must return to coder for a narrow fail-closed resolver fix before test handoff
- Coder repaired the PR1 fidelity blocker in follow-up commit `4a30e9c85912cf53480255a694119b29b76ab3b8` (`[CODE]: manifest fail closed implicit interp root`):
  - `scripts/generate_manifest.py` now treats an absolute `PT_INTERP` with no explicit `--search-root` as a hard tool error instead of probing the host root filesystem
  - refreshed bounded proofs now include a positive explicit-sysroot manifest emission at `logs/coder/TASK_20260421_004333_manifest_mvp_pr1_manifest_smoke_20260421_111813.log` and a negative fail-closed proof at `logs/coder/TASK_20260421_004333_manifest_mvp_pr1_failclosed_20260421_111849.log`
- Reviewer re-check approved the repaired PR1 route for test handoff:
  - direct reviewer checks on the current `scripts/generate_manifest.py` reproduced `python3 -m py_compile`, a positive explicit-sysroot generation for `/tmp/nacc_manifest_smoke_20260421_111813.elf`, and a negative fail-closed `/bin/ls` run with no `--search-root`
  - no new fidelity or risk blocker was found beyond intentionally deferred PR1 scope limits such as broader runtime DSO discovery and later guest/monitor plumbing
- Test runner independently reran the bounded PR1 T1 checks and recorded a passing runner-owned artifact set:
  - `python3 -m py_compile scripts/generate_manifest.py` passed
  - explicit-sysroot generation passed and wrote `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr1_t1_20260421_113715.manifest.json` with `schema=nacc-manifest-v1alpha1`, `roles=entry,interp`, and interpreter resolution under `/home/link/NaCC/riscv-tools/sysroot`
  - the no-`--search-root` `/bin/ls` negative case failed closed with exit `1` and produced no output file; summary log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr1_t1_20260421_113715.log`
- Human approved PR1 closeout after reviewing the changes and explicitly requested that continuation stay on this execution packet instead of opening a separate lane-B planning packet.
- Human checkpoints are restored for PR boundaries on this packet: keep automatic machine-to-machine flow inside a PR, but when a PR-sized slice is intentionally parked with `Next owner: human`, organizer should pause for explicit human continuation.
- PR2 was the active slice:
  - deliver a caller-supplied `manifest.json` into `NaCC.qcow2` at fixed guest path `/etc/nacc/manifest.json`
  - reuse the existing pre-boot `qemu-nbd` + mounted-`rootfs` workflow already used for other image updates; do not mutate the qcow2 while QEMU is running
  - keep this PR behavior-neutral: no Linux/OpenSBI/agent consumer, no schema change, no `manifest.bin`, no runtime `scp`/`ssh`/9p/virtiofs delivery path, and no runtime load-base reporting
  - validation target for PR2 is bounded: a host-generated manifest is installed, the guest boots, and the guest-visible file at `/etc/nacc/manifest.json` matches the host artifact by hash and/or bounded content readback
- PR2 coder implementation is now ready for review:
  - top-level commit `261971b58ff653c6e1818fe4cd28af4c1691037e` (`[CODE]: manifest add guest delivery helper`) adds `scripts/install_manifest.py`, a standalone pre-boot host-side installer for `/etc/nacc/manifest.json`
  - the helper reuses the repo's `qemu-nbd` + mounted-`rootfs` flow, refuses to proceed when a live `qemu-system-riscv64` still appears to own `NaCC.qcow2`, copies a caller-supplied manifest unchanged to the fixed guest path, and compares the source and installed guest file by SHA-256 before cleanup
  - bounded local artifacts for this slice are `logs/coder/TASK_20260421_004333_manifest_mvp_pr2_manifest_source_20260421_134432.json`, `logs/coder/TASK_20260421_004333_manifest_mvp_pr2_manifest_source_20260421_134432.log`, and the current-host privilege blocker capture `logs/coder/TASK_20260421_004333_manifest_mvp_pr2_install_blocked_host_20260421_134432.log`
  - direct live qcow2 mutation could not be completed on this host because the repo-aligned `ROOT_SUDO` default (`sudo -n`) lacks passwordless privilege here; the preserved blocker log records `sudo -n modprobe nbd max_part=16` failing with `sudo: a password is required`
- Reviewer approved PR2 for bounded test handoff:
  - direct code review confirmed that commit `261971b58ff653c6e1818fe4cd28af4c1691037e` adds only `scripts/install_manifest.py`, keeps Linux/OpenSBI/agent untouched, reuses the repo-owned `ROOT_SUDO` + `qemu-nbd` + mounted-`rootfs` path, and keeps `/etc/nacc/manifest.json` as the fixed guest destination
  - direct reviewer reruns reproduced `python3 -m py_compile scripts/install_manifest.py`, and a live invocation failed early before disk mutation in the current sandbox because root escalation is unavailable here; no PR2 fidelity or code-level risk blocker was found
- Log analysis classified the first PR2 T1 install blocker as a false-positive live-QEMU owner match:
  - runner log `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr2_t1_20260421_153035.log` shows the rejected pid command line begins `node /usr/bin/codex -C /home/link/NaCC ...`, not a real `qemu-system-riscv64` launch
  - `scripts/install_manifest.py:86-116` matches any process whose full `ps` args merely contain `qemu-system-riscv64` and `NaCC.qcow2` text plus a repo-root cwd, which allowed prompt-carrying Codex worker text to satisfy the guard
  - the failure occurred before `modprobe`, `qemu-nbd -c`, or `mount`, so no qcow2 mutation or guest boot was attempted in the runner-owned pass
- Coder repaired the PR2 live-QEMU guard in follow-up commit `7fa1a4e660552df2f7ce90b8a407cc441d5aad71` (`[CODE]: manifest tighten qemu owner detection`):
  - `scripts/install_manifest.py` now inspects `/proc` directly and only treats a pid as a live guest owner when `/proc/<pid>/exe` identifies a real `qemu-system-riscv64` executable; the existing disk-path and repo-cwd heuristics run only after that positive identity check
  - bounded local guard proof in `logs/coder/TASK_20260421_004333_manifest_mvp_pr2_guard_sanity_20260421_153746.log` shows a non-QEMU python process carrying `qemu-system-riscv64` plus `NaCC.qcow2` text is ignored, while a minimal paused real QEMU process referencing `NaCC.qcow2` is still detected
  - bounded local recheck `logs/coder/TASK_20260421_004333_manifest_mvp_pr2_guard_fix_install_blocked_host_20260421_153746.log` now reaches the known host privilege blocker at `sudo -n modprobe ...` instead of failing at the stale false-positive owner guard
- Reviewer re-check approved the PR2 live-QEMU guard repair for renewed bounded test handoff:
  - direct review of commit `7fa1a4e660552df2f7ce90b8a407cc441d5aad71` confirmed the diff stays inside `scripts/install_manifest.py` and keeps the PR2 route delivery-only, with no Linux/OpenSBI/agent or manifest-consumer change
  - direct reviewer reruns reproduced `python3 -m py_compile scripts/install_manifest.py`, and a live invocation now reaches the expected local `sudo -n modprobe ...` privilege blocker instead of the stale false-positive owner trap
  - direct code checks against `Makefile:289` and `Makefile:334` confirmed the repo's standard launch path still execs `qemu-system-riscv64` directly, so the new `/proc/<pid>/exe` identity rule fits the intended control model for PR2 validation
- Test runner reran the bounded PR2 T1 loop after the guard fix and hit the expected host privilege boundary:
  - `python3 -m py_compile scripts/install_manifest.py` passed
  - `sha256sum logs/coder/TASK_20260421_004333_manifest_mvp_pr2_manifest_source_20260421_134432.json` matched the packet expectation `010a63ae4059068f55daa6734e601c2f2c709b1f2de699e65fbc9e09f7fb2a30`
  - `python3 scripts/install_manifest.py logs/coder/TASK_20260421_004333_manifest_mvp_pr2_manifest_source_20260421_134432.json` failed at `sudo -n modprobe nbd max_part=16` with `sudo: a password is required`, so the run stopped before `qemu-nbd -c`, `mount`, guest boot, or logger
  - runner-owned primary log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr2_t1_20260421_154938.log`
  - post-failure cleanliness checks in the same runner log show `rootfs` was not mounted and `/dev/nbd0` was not present after the failed install attempt
- PR2 remains blocked on host execution privilege rather than code behavior:
  - the current validating host still lacks passwordless `ROOT_SUDO` access for the repo-owned pre-boot `qemu-nbd` workflow
  - guest boot plus `/etc/nacc/manifest.json` readback is still pending on a root-capable host and remains the next required evidence before PR2 can close
- Test runner retried the same bounded PR2 T1 loop after the user indicated sudo access might be updated:
  - `python3 -m py_compile scripts/install_manifest.py` passed again
  - `sha256sum logs/coder/TASK_20260421_004333_manifest_mvp_pr2_manifest_source_20260421_134432.json` still matched `010a63ae4059068f55daa6734e601c2f2c709b1f2de699e65fbc9e09f7fb2a30`
  - `python3 scripts/install_manifest.py logs/coder/TASK_20260421_004333_manifest_mvp_pr2_manifest_source_20260421_134432.json` still failed immediately at `sudo -n modprobe nbd max_part=16` with `sudo: a password is required`
  - runner-owned retry log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr2_t1_20260421_162645.log`
  - cleanup checks in the retry log again show `rootfs` not mounted and `/dev/nbd0` absent after the failed attempt
- Test runner reran the bounded PR2 T1 loop after `sudo -n` access was fixed and reached the guest-readback stage:
  - `python3 -m py_compile scripts/install_manifest.py` passed again
  - `sha256sum logs/coder/TASK_20260421_004333_manifest_mvp_pr2_manifest_source_20260421_134432.json` still matched `010a63ae4059068f55daa6734e601c2f2c709b1f2de699e65fbc9e09f7fb2a30`
  - `python3 scripts/install_manifest.py logs/coder/TASK_20260421_004333_manifest_mvp_pr2_manifest_source_20260421_134432.json` succeeded and reported installation to guest path `/etc/nacc/manifest.json`
  - runner launched a fresh tmux debug session, manually sent one `gdb` `c` after the session came up so the paused `-S -s` debug launch would run, and waited through the packet-owned guest-readback window after `[NaCC] Auto-running: ...`
  - the guest boot reached SSH auto-run, but no guest-visible `sha256sum /etc/nacc/manifest.json` output and no manifest `schema` or `role` readback appeared within the bounded wait window
  - runner-owned primary log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr2_t1_20260421_164341.log`
  - runner-owned guest/QEMU artifacts: `logs/TASK_20260421_004333_manifest_mvp_pr2_t1_20260421_164341_vm_20260421_164945.log` and `logs/TASK_20260421_004333_manifest_mvp_pr2_t1_20260421_164341_qemu_20260421_164945.log`
  - runner cleaned the dedicated tmux session and confirmed `qemu_process_count_after_kill=0`
- Log analysis classified the latest PR2 readback failure as a host-side SSH auto-run/session boundary failure, not a manifest install failure:
  - `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr2_t1_20260421_164341.log` shows install success, `auto_running_seen=1`, `digest_seen=0`, `schema_or_role_seen=0`, and no VM-side error match before cleanup
  - `logs/TASK_20260421_004333_manifest_mvp_pr2_t1_20260421_164341_vm_20260421_164945.log` and `logs/live_vm_pane_182.log` both stop immediately after the SSH host-key warning, with no manifest hash, no schema/role output, no retry message, and no shell prompt return
  - `logs/TASK_20260421_004333_manifest_mvp_pr2_t1_20260421_164341_qemu_20260421_164945.log` shows normal boot through serial login readiness and no obvious guest crash or manifest-related kernel fault in the captured window
  - repository evidence in `config/vm_link.sh` shows the VM readiness gate only waits for an SSH banner on `localhost:2222` before it immediately launches `sshpass -p riscv ssh -tt ... "$AUTO_CMD"`, so the first observed bad boundary is after banner detection and before first remote command output
- Coder implemented the requested PR2 SSH harness follow-up in commit `b46309c4aac2f7015d4139a65ef038011afc12f2` (`[CODE]: vm link probe authenticated ssh`):
  - `config/vm_link.sh` keeps the existing banner wait, `sshpass` fallback, retry-on-255 loop, and auto-run path intact, but adds an authenticated readiness probe that loops on `ssh ... true` before the script trusts the SSH session as usable
  - the same file now bounds auto-run hangs with explicit `[NaCC][ssh-auto-timeout]` / `[NaCC][ssh-auto-exit]` logging so reviewer/test_runner can distinguish session stalls from remote command failure without guessing from a silent pane
  - to avoid hard-breaking longer pre-existing manual or batch flows, the new bounds are minimally overridable through `VM_SSH_READY_TIMEOUT_SECONDS` and `VM_SSH_AUTO_TIMEOUT_SECONDS`; default behavior stays on the same `vm_link.sh` control path
- Bounded coder validation for the SSH follow-up stayed host-side and syntax-only:
  - `bash -n config/vm_link.sh` passed with preserved log `logs/coder/TASK_20260421_004333_manifest_mvp_pr2_vm_link_syntax_20260421_170108.log`
  - live guest install/boot/readback remains runner-owned because this turn intentionally changed only the host-side readback harness and did not rerun the packet's full T1 loop locally
- Reviewer approved the PR2 SSH follow-up for renewed bounded test handoff:
  - direct review of commit `b46309c4aac2f7015d4139a65ef038011afc12f2` confirmed the diff stays inside `config/vm_link.sh` and preserves the standard `make debug -> make vm -> ./config/vm_link.sh` control path used for PR2 readback
  - `config/vm_link.sh` now adds only the packet-requested authenticated `ssh ... true` readiness probe plus `[NaCC][ssh-ready{-timeout}]` and `[NaCC][ssh-auto-{timeout,exit}]` diagnostics, while leaving the existing banner wait and retry-on-255 loop in place
  - direct reviewer rechecks reproduced `bash -n config/vm_link.sh`; no new PR2 fidelity or code-level risk blocker was found, though runner should keep validation on the standard `sshpass`-backed flow and only relax the new timeout knobs if the bounded command legitimately needs more time
- Test runner reran the bounded PR2 T1 loop after the SSH probe follow-up and recorded runner-owned in-guest readback proof:
  - `bash -n config/vm_link.sh` passed and `python3 -m py_compile scripts/install_manifest.py` passed
  - `sha256sum logs/coder/TASK_20260421_004333_manifest_mvp_pr2_manifest_source_20260421_134432.json` still matched `010a63ae4059068f55daa6734e601c2f2c709b1f2de699e65fbc9e09f7fb2a30`
  - `python3 scripts/install_manifest.py logs/coder/TASK_20260421_004333_manifest_mvp_pr2_manifest_source_20260421_134432.json` succeeded and left `rootfs` unmounted afterward
  - the standard `make debug VM_AUTO_CMD='sha256sum /etc/nacc/manifest.json; grep -E "\"schema\"|\"role\"" /etc/nacc/manifest.json || true; test -f /etc/nacc/manifest.json'` flow reached guest auto-run, the primary runner log recorded `auto_exit_code=0`, and the paired VM log captured guest-visible digest `010a63ae4059068f55daa6734e601c2f2c709b1f2de699e65fbc9e09f7fb2a30  /etc/nacc/manifest.json`
  - direct post-run cleanup checks found no live `qemu-system-riscv64` process, `rootfs` not mounted, `/dev/nbd0` present only as a detached `0B` device node, and no `/dev/nbd0p1`
- Human inspected the current PR2 evidence in-session and approved continuation:
  - PR2 guest delivery may now be treated as closed on this packet
  - continuation should stay on this same execution packet and move directly to the next bounded PR-sized slice
- PR3 is now the active slice:
  - add the smallest startup-only runtime-coordinate report for the fresh image before first user entry
  - Linux may report raw coordinates, but they remain coordinates only; OpenSBI may store/log them, but must not yet compare them against the manifest
  - keep this PR scoped to the current PR1 object set (`entry` and optional `interp`) and to existing startup transitions; do not widen into manifest parsing, audit verdicts, or enforcement
  - the preserved PR2-installed manifest remains a delivery proof only in this slice; workload-matched manifest selection is deferred to PR4/PR5
- PR3 coder implementation is now ready for review:
  - Linux commit `00b914f9fde454dd580aec23bb98ccea46d13a21` (`[CODE]: riscv nacc report startup runtime coords`) adds startup-coordinate cache/report plumbing across `fs/binfmt_elf.c`, `arch/riscv/mm/nacc.c`, `arch/riscv/kernel/sys_riscv.c`, and the RISC-V NaCC/mmu/SBI headers
  - OpenSBI commit `7749352da4b272260677179b960702351f7c7e86` (`[CODE]: sm store startup runtime coords`) adds the role-based startup-coordinate store/log path across `include/sm/region.h`, `include/sm/sm.h`, `lib/sbi/sbi_ecall_nacc.c`, `lib/sbi/sm/region.c`, and `lib/sbi/sm/sm.c`
  - route chosen: Linux caches `entry_load_bias`, `interp_load_addr`, `AT_ENTRY`, and `AT_PHDR` on the fresh exec `mm` in `load_elf_binary()`, using an explicit `interp_present` boolean rather than inferring interpreter absence from `interp_load_addr == 0`; Linux then reports only role-tagged raw runtime bases for `entry` and optional `interp` through a new `SBI_EXT_NACC_STARTUP_COORD` call on the existing startup transitions `nacc_invoke`, `nacc_exec`, and `nacc_invoke_child`
  - OpenSBI stores only `startup_coord_valid_mask`, `startup_entry_load_bias`, and `startup_interp_load_addr` per authorized root/cid, and its logs state explicitly that PR3 is coordinates-only with no startup policy change
- Bounded coder build proof for PR3:
  - `make opensbi` passed with preserved log `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_make_opensbi_20260421_232805.log`
  - Linux kernel image rebuild passed with preserved log `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_linux_build_20260421_230143.log`
  - `make final-image` passed with preserved log `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_final_image_20260421_230616.log`
  - `make linux-update` was attempted in-sandbox and escalated; in both cases the kernel/modules build completed through module install, but the final `modules-update-wrapper` step failed for operational reasons rather than code compile failure: first because sandboxed `sudo` was blocked (`logs/coder/TASK_20260421_004333_manifest_mvp_pr3_make_linux_update_20260421_232805.log`), then because a coder-owned debug QEMU still held `NaCC.qcow2` during the escalated rerun (`logs/coder/TASK_20260421_004333_manifest_mvp_pr3_make_linux_update_escalated_20260422_000124.log`)
- Bounded coder runtime attempt for PR3:
  - the first local smoke launch hit a stale paused `make debug` / QEMU owner from `17:13`, preserved in `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_runtime_qemu_20260421_233444.log`; coder explicitly inspected and cleared that stale owner before retrying
  - a fresh standard `make debug VM_AUTO_CMD='docker run --security-opt seccomp=unconfined --rm busybox echo test'` session reached authenticated SSH readiness and auto-run start, preserved in `logs/live_vm_pane_234.log`
  - direct SSH probe to the same forwarded guest succeeded after sandbox escalation, but two exact smoke attempts preserved in `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_runtime_ssh_smoke_20260421_234703.log` and `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_runtime_ssh_smoke_retry_20260421_234703.log` both failed because the guest Docker daemon was not yet ready, so coder did not obtain a reviewer-ready Linux/OpenSBI coordinate-log pair in this turn
- Reviewer approved PR3 for bounded test handoff:
  - direct code review confirmed that `linux/fs/binfmt_elf.c` caches fresh-image coordinates from the existing ELF exec path, `linux/arch/riscv/kernel/sys_riscv.c` reports only role-tagged raw bases on the existing startup transitions `nacc_invoke`, `nacc_exec`, and `nacc_invoke_child`, and `opensbi/lib/sbi/sm/region.c` stores only the authorized `entry` / `interp` runtime bases plus a valid-mask per root/cid
  - direct reviewer checks found no manifest read/parse/compare path, no audit verdict path, no new leaf-tag decision path, and no startup-policy drift in this slice; the remaining work is runner-owned runtime evidence that the Linux/OpenSBI coordinate logs appear together on a fresh startup event
- Test runner executed the bounded PR3 T1 loop on `2026-04-22` and stopped at the first runner-owned failing runtime artifact:
  - `make opensbi` passed with runner-owned log `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_make_opensbi_20260422_002416.log`
  - `make linux-update` passed with runner-owned log `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_make_linux_update_20260422_002416.log`
  - a fresh detached standard `make debug VM_AUTO_CMD='docker run --security-opt seccomp=unconfined --rm busybox echo test'` run never reached `[NaCC] Auto-running:` within the bounded wait, so the packet stopped before PR3 coordinate verification
  - runner-owned primary log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_002416.log`
  - runner-owned runtime artifacts: `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_002416_vm_20260422_003713.log` and `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_002416_qemu_20260422_003713.log`
- Coder inspected the runner-owned PR3 stall and classified it as a host debug auto-continue race rather than a fresh PR3 runtime regression:
  - `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_002416.log` records no manual `gdb continue` action, unlike earlier passing runner flows such as `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr2_t1_20260421_163626.log`
  - the paired runner-owned VM/QEMU artifacts show the exact paused-debug signature for this repo path: VM side never leaves the SSH wait loop and QEMU side never prints beyond `[NaCC][qemu-run-start]`
- Coder repaired the PR3 pre-auto-run stall in follow-up commit `3088363bda30efc7f43cdd9d50fe76245c3b669e` (`[CODE]: debug wait for gdb prompt before continue`):
  - `config/tmux-debug.sh` now waits until the `nacc-gdb` pane reaches a real `(gdb)` prompt before sending the existing `c`, instead of racing a fixed `sleep 2`
  - the fix stays on the existing `make debug -> config/tmux-debug.sh -> make gdb / make launch DEBUG=1` control path and does not change Linux/OpenSBI PR3 logic, manifest handling, or startup policy
  - bounded sanity logs for this harness-only follow-up are `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_syntax_20260422_004452.log` and `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_diffcheck_20260422_004452.log`
- Reviewer approved the PR3 harness follow-up for renewed bounded test handoff:
  - direct review of commit `3088363bda30efc7f43cdd9d50fe76245c3b669e` confirmed the needed change remains in `config/tmux-debug.sh`: the script still launches the same three panes, still starts QEMU under `-S -s`, and still auto-sends a single `c`, but now waits for a real `(gdb)` prompt before sending it
  - direct reviewer checks matched the paused-debug diagnosis against the failing runner artifacts `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_002416.log`, `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_002416_vm_20260422_003713.log`, and `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_002416_qemu_20260422_003713.log`, and reproduced `bash -n config/tmux-debug.sh` plus `git diff --check` on the follow-up diff
  - no new fidelity or code-level risk blocker was found; the remaining work is a fresh runner-owned PR3 T1 rerun on the standard `make debug` path without manual `gdb continue` injection on the first pass
- Test runner executed the renewed PR3 T1 rerun on `2026-04-22` and stopped at the first fresh runner-owned failing artifact after the harness follow-up:
  - no packet-owned rebuilds were rerun on this pass; `linux/`, `opensbi/`, and `qemu/` were clean in direct runner checks, while the active handoff change remained the host-side `config/tmux-debug.sh` follow-up already reviewed for this rerun
  - fresh detached session `tr_pr3_20260422_005709` created `nacc-vm` and `nacc-gdb` panes, but no live `nacc-qemu` pane appeared in the bounded startup wait
  - runner-owned VM log `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_005709_vm_20260422_010417.log` never reached `[NaCC] Auto-running: ...`
  - runner-owned logger artifact `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_005709_qemu_20260422_010417.log` contains no `[NaCC][qemu-run-start]` marker for this pass, and direct runner captures preserved the paired GDB pane plus pane inventory in `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_005709_gdb_20260422_010448.log` and `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_005709_panes_20260422_010448.log`
  - runner-owned primary log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_005709.log`
- Coder reproduced the fresh detached-session failure locally on the host harness path without rerunning the full packet loop:
  - a bounded detached probe `make debug VM_AUTO_CMD='echo probe'` reproduced the same two-pane state with no live `nacc-qemu` pane
  - a direct bounded `timeout 5 make launch DEBUG=1` showed the immediate launch failure `Failed to get "write" lock` on `NaCC.qcow2`, proving the missing pane was caused by a fast QEMU exit rather than a silent tmux split failure
  - `lsof /home/link/NaCC/NaCC.qcow2` and `ss -ltnp` confirmed a live real `qemu-system-riscv64` owner (`pid 3703684`) still held `NaCC.qcow2` plus ports `2222` and `1234`
  - tmux pane inventory also showed a live repo debug session `tr_pr3_20260422_005356` still carrying `nacc-qemu`, so the first fresh boundary is now classified as a stale-owner collision on the host debug path
- Coder implemented the next narrow PR3 harness follow-up in commit `65df34b0cece1343fadf9c0426a2c7298a7d5df8` (`[CODE]: debug guard stale qemu owner`):
  - `config/tmux-debug.sh` now sets the bottom pane title early and enables `remain-on-exit` so immediate launch failures preserve a visible `nacc-qemu` pane instead of collapsing to a missing-pane artifact
  - the same file now scans `/proc` for a real `qemu-system-riscv64` owner of `NaCC.qcow2` before splitting/launching, and emits explicit `[NaCC][qemu-owner-block]` lines with the owning pid/command when the disk is still busy
  - the GDB helper in the same file now steps through the startup pager prompt (`--Type <RET>...`) before waiting for `(gdb)`, so the later auto-continue still sends the actual inferior `c` on a clean host
  - bounded coder sanity for this follow-up is preserved in `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_syntax_20260422_011516.log`, `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_diffcheck_20260422_011516.log`, `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_owner_guard_20260422_011516.log`, and `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_owner_guard_panes_20260422_011516.log`
- Reviewer found one narrow PR3 harness fidelity blocker in commit `65df34b0cece1343fadf9c0426a2c7298a7d5df8`:
  - the stale-owner guard and early `nacc-qemu` pane preservation stay within the intended host debug control path, but the new pager matcher in `config/tmux-debug.sh` does not actually detect the GDB pager prompt
  - direct reviewer rerun of `printf '%s\n' "$GDB_PAGER_PROMPT" | grep -Fq "$GDB_PAGER_PROMPT"` exits `2` with `grep: unrecognized option '--Type <RET> ...'` because the pattern begins with `--Type`, and the real pane capture in `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_005709_gdb_20260422_010448.log` also shows the pager prompt wrapped across lines, so the current exact single-line match is not robust
  - because this follow-up explicitly claimed to fix both the stale-QEMU-owner collision and the GDB pager edge before the next runner-owned rerun, the broken pager path is treated as a spec-fidelity blocker rather than a style nit
- Coder repaired the narrow PR3 pager-path fidelity blocker in a host-harness-only follow-up to `config/tmux-debug.sh`:
  - `send_gdb_continue_when_ready()` now inspects only the current tmux pane screen instead of full scrollback, so stale pager text from earlier screen state does not keep matching after GDB resumes
  - the helper now compacts whitespace before matching `--Type <RET> for more, q to quit, c to continue without paging--`, which makes the real wrapped pane shape in `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_005709_gdb_20260422_010448.log` match reliably without treating the prompt as a `grep` option
  - the helper now prefers a visible `(gdb)` prompt over any residual pager text, so a prompt-bearing screen still sends the intended inferior `c` instead of looping on raw pager clears
  - bounded coder sanity for this repair is preserved in `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_syntax_20260422_013318.log`, `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_diffcheck_20260422_013318.log`, and `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_pager_mock_20260422_013402.log`
- Reviewer recheck approved the PR3 wrapped-pager harness repair for renewed bounded test handoff:
  - direct review of `config/tmux-debug.sh` confirmed `send_gdb_continue_when_ready()` still stays inside the existing `make debug -> config/tmux-debug.sh -> make gdb / make launch DEBUG=1` control path and that the only new logic in this commit is visible-pane capture, whitespace-compacted pager matching, and prompt-first continue handling
  - direct reviewer checks reproduced `bash -n config/tmux-debug.sh`, confirmed that the real wrapped pager prompt from `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_005709_gdb_20260422_010448.log` matches the new compact-whitespace path while `(gdb)` does not yet appear on that screen, and found no new Linux/OpenSBI, manifest, startup-policy, or guest-lifecycle drift in this follow-up
  - remaining work is the same runner-owned PR3 T1 rerun on a clean host with no manual `gdb continue` injection on the first pass
- Test runner executed the renewed PR3 T1 rerun on `2026-04-22` after clearing stale debug state from the previous failed attempts:
  - no packet-owned rebuilds were rerun on this pass; `linux/`, `opensbi/`, and `qemu/` were clean in direct runner checks, while the active handoff change remained the reviewed host-side `config/tmux-debug.sh` follow-up
  - runner cleared the stale debug window and lingering repo-owned `qemu-system-riscv64`, then confirmed `prelaunch_qemu_processes=0` and no listeners on forwarded ports `2222` / `1234` before launch
  - fresh detached session `tr_pr3_20260422_114015` created live `nacc-qemu`, `nacc-vm`, and `nacc-gdb` panes, so the previous missing-pane failure mode did not recur on this rerun
  - the bounded wait still never reached `[NaCC] Auto-running:`, so the runner stopped at the first fresh failing boundary `autorun_not_observed` without manual `gdb continue`
  - runner-owned primary log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_114015.log`
  - runner-owned runtime artifacts: `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_114015_vm_20260422_114819.log`, `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_114015_qemu_20260422_114819.log`, `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_114015_gdb_20260422_114820.log`, and `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_114015_panes_20260422_114820.log`
  - cleanup completed after capture: dedicated tmux session was killed and `qemu_process_count_after_kill=0`
- Log analysis classified the latest PR3 rerun as a repeated paused-debug harness failure, not fresh PR3 runtime-coordinate evidence:
  - the primary runner log records `auto_running_seen=0`, `qemu_start_marker=1`, zero Linux/OpenSBI coordinate lines, and final `verdict_reason=autorun_not_observed` (`logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_114015.log:19-40`)
  - the paired VM pane never leaves `Waiting for VM (localhost:2222) to operate...` (`logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_114015_vm_20260422_114819.log:6-11`)
  - the paired QEMU log contains only `[NaCC][qemu-run-start]` and no OpenSBI/Linux boot text (`logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_114015_qemu_20260422_114819.log:1-9`)
  - the paired GDB pane shows the wrapped startup pager plus many injected `c` characters, then ends parked at `(gdb)` (`logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_114015_gdb_20260422_114820.log:16-18,51`)
  - current repo state now includes an uncommitted `Makefile` update on the `make gdb` path that adds `-q -iex "set pagination off"` (`Makefile:349-352`), so the older packet inference that pagination is only missing from `config/.gdbinit` is stale relative to the workspace; this is post-run context, not runner-owned evidence
- Refined log analysis after rechecking the live harness files:
  - the runner-owned GDB pane never shows `Continuing.`, so the strongest direct evidence is still “inferior continue not observed,” not just “pager looked suspicious” (`logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_114015_gdb_20260422_114820.log`)
  - current repo state shows `config/.gdbinit:1-8` still lacks `set pagination off`, while `Makefile:349-352` now adds `-q -iex "set pagination off"` on the `make gdb` path; treat that Makefile tweak as post-run workspace context, not proof about the failing runner pass
  - `config/tmux-debug.sh:44-68` still sends pager-clear / continue keys without any post-send verification that GDB printed `Continuing.` or that QEMU advanced beyond the paused `-S -s` start, so the next narrow coder slice should make that boundary observable before another runner-owned rerun
- Coder validated the live `make gdb` pagination-off attempt directly before patching:
  - bounded PTY `make gdb` preserved in `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_gdb_nopager_20260422_121500.log` reaches `(gdb)` without any `--Type <RET>...` pager prompt, so the remaining ambiguity was no longer “does pagination-off work at all” but “can the committed harness state prove a real inferior continue”
- Coder implemented the next narrow PR3 harness follow-up in the committed debug config:
  - `config/.gdbinit` now sets `pagination off`, so the standard GDB startup path no longer depends on the workspace-only uncommitted `Makefile` `-iex "set pagination off"` addition
  - `config/tmux-debug.sh` now sends an explicit GDB-side marker before the single auto-continue and waits for visible `Continuing.`; if that proof does not appear, it emits explicit `[NaCC][gdb-ready-timeout]` / `[NaCC][gdb-continue-unconfirmed]` diagnostics instead of silently injecting keys
- Bounded coder proof for this harness follow-up:
  - `bash -n config/tmux-debug.sh` passed with preserved log `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_syntax_20260422_121500.log`
  - `git diff --check -- config/.gdbinit config/tmux-debug.sh` passed with preserved log `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_diffcheck_20260422_121500.log`
  - bounded PTY direct `./riscv-tools/bin/riscv64-unknown-linux-gnu-gdb -x config/.gdbinit` preserved in `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_gdbinit_nopager_20260422_121500.log` shows the committed `.gdbinit` alone reaches `(gdb)` without the startup pager
  - escalated bounded tmux mock preserved in `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_continue_mock_20260422_121500.log` shows the pane contains both `[NaCC][gdb-auto-continue]` and `Continuing.`, while the helper logs `[NaCC][gdb-continue-observed]`
- Reviewer approved the committed GDB auto-continue follow-up for renewed PR3 test handoff:
  - direct review of commit `eea351406806527994946a22b9ca0e13768d0b30`, `config/.gdbinit`, and `config/tmux-debug.sh` confirmed that the change stays entirely on the host debug path and leaves the accepted Linux/OpenSBI PR3 runtime-coordinate commits `00b914f9fde454dd580aec23bb98ccea46d13a21` and `7749352da4b272260677179b960702351f7c7e86` untouched
  - direct reviewer rechecks reproduced `bash -n config/tmux-debug.sh` and a live `./riscv-tools/bin/riscv64-unknown-linux-gnu-gdb -x config/.gdbinit` startup reaching `(gdb)` without the startup pager, aside from the expected local `target remote:1234` permission failure under the current sandbox
  - comparison against the prior failing runner pane `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_114015_gdb_20260422_114820.log` plus the new coder proof `logs/coder/TASK_20260421_004333_manifest_mvp_pr3_tmux_debug_continue_mock_20260422_121500.log` shows the committed route now leaves a concrete post-continue marker instead of relying on silent key injection
  - no new fidelity or code-level risk blocker was found; the next required evidence remains a fresh runner-owned PR3 T1 rerun on a clean host with no manual `gdb continue` injection on the first pass
- Test runner executed the renewed PR3 T1 rerun on `2026-04-22` and preserved a fresh runner-owned artifact set that crossed the pre-auto-run boundary:
  - runner cleared one live repo-owned `qemu-system-riscv64` owner of `NaCC.qcow2` before launch, then ran the standard packet command `make debug VM_AUTO_CMD='docker run --security-opt seccomp=unconfined --rm busybox echo test'` with no manual `gdb continue` injection
  - runner-owned VM artifact `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_130503_vm_20260422_131203.log` shows authenticated SSH readiness, `[NaCC] Auto-running: docker run --security-opt seccomp=unconfined --rm busybox echo test`, and guest output `test`
  - runner-owned QEMU artifact `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_130503_qemu_20260422_131203.log` shows one Linux startup report plus two matching OpenSBI startup-coordinate lines for the same startup event: `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_130503_qemu_20260422_131203.log:1869-1871`
  - direct runner grep on the same QEMU artifact found `linux_report_count=1`, `sbi_coord_count=2`, and `manifest_audit_or_enforce_count=0`
  - the primary runner log `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_130503.log` retained stale live-poll fields (`auto_running_seen=0`, `wait_reason=autorun_not_observed`) that do not match the preserved VM/QEMU artifacts, and the paired runner-owned GDB capture path `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_130503_gdb_20260422_131204.log` is empty
  - post-capture cleanup required an extra precise kill of the lingering repo-owned QEMU process; final owner check returned none and forwarded ports `2222` / `1234` were clear
- Log analysis reduced the latest PR3 runner-owned pass and classified it as human-closeout-ready rather than a fresh PR3 failure:
  - `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_130503_qemu_20260422_131203.log:1-2` records `[NaCC][qemu-run-start]` plus `[NaCC][gdb-continue-observed]`, so this rerun crossed the paused-debug boundary on the accepted harness path
  - `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_130503_vm_20260422_131203.log:10-21` shows authenticated SSH readiness, `[NaCC] Auto-running: ...`, and guest output `test`
  - `logs/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_130503_qemu_20260422_131203.log:1868-1871` shows `manifest scaffold mode=off`, one Linux startup report, and two matching OpenSBI startup-coordinate lines; no manifest-audit or enforcement evidence appears in the preserved artifacts
  - the only remaining mismatch is harness metadata: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_130503.log:16-31` still reports stale live-poll fields and `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr3_t1_20260422_130503_gdb_20260422_131204.log` is empty, but those collection artifacts do not outweigh the preserved VM/QEMU runtime evidence
- Human has now indicated that PR3 may be treated as passed for this packet and that progression toward PR4 is acceptable, with one explicit process guard:
  - this turn may ACK the packet forward to `planner`
  - however, planner must first discuss the PR4 route and boundaries with the human instead of immediately driving a machine-to-machine handoff chain
  - planner must not immediately ACK onward on receipt of this packet without that human discussion
- Planner has now checked the current repo state for PR4 shaping:
  - `scripts/generate_manifest.py` already provides role-tagged `PT_LOAD` facts for the current PR1 object set (`entry` and optional `interp`)
  - PR2 already proved guest delivery of `/etc/nacc/manifest.json`, and PR3 already proved runtime coordinate capture/store plus startup logging for the same object roles
  - current code search found no existing NaCC runtime manifest reader or JSON parser on the Linux/OpenSBI startup path, so PR4 still needs an explicit consumer-path decision rather than assuming one already exists
- Human direction for this planner turn is now explicit:
  - discuss PR4 more properly before any coder handoff
  - do not ACK onward to `coder` yet
  - keep the remaining PR4 route choices explicit in the packet until the human freezes them
- Human has now supplied first PR4 route preferences:
  - long-term manifest handling should preferably happen near container/CID registration, reusing the kind of registration-style path OpenSBI already has for container state, instead of treating early startup file parsing as the final architecture
  - a later implementation may involve runtime/container code reading the manifest from the guest filesystem and passing it along that registration path; the human expects this may touch runtime code such as `runC`
  - for PR4 MVP, the work may stay minimal and use the manifest artifact already written into the guest rootfs plus the corresponding ELF for study and first flow bring-up, rather than introducing SCP-driven manifest delivery immediately
  - the semantic question of whether current PR1 manifest contents are rich enough to replace Linux VMA-derived private/shared startup classification is still open and should be discussed further before freezing PR4/PR5 behavior
- Planner has now frozen the provisional PR4 MVP route for human review:
  - PR4 should only bring up a startup-only audit loop, not final policy replacement
  - PR4 may temporarily consume the already installed guest manifest artifact at `/etc/nacc/manifest.json` as an MVP mechanism
  - PR4 should use the ELF corresponding to that installed manifest artifact as the first proof target
  - PR4 should reconstruct expected `PT_LOAD` ranges for `entry` and optional `interp` from the manifest plus PR3 runtime coordinates, then emit audit evidence before first user entry
  - the long-term registration/CID manifest-ingestion architecture is explicitly deferred to PR6 or later
- Human has now asked the planner to make PR4 validation explicit:
  - PR4 should not stop at “implementation exists”; it needs a concrete correctness-validation step
  - planner should explain how PR4 correctness is checked, not just how the code is wired
- Human has now approved the concrete PR4 MVP route:
  - keep PR4 as the minimal startup-only audit loop
  - validation should include a positive matched case and a bounded mismatch case
  - planner should split PR4 into smaller execution steps so implementation and testing roles do not have to carry the whole context at once

- PR4 is now the active slice:
  - PR4A `startup-table prep`: derive a compact startup-only table from the existing manifest facts for `entry` and optional `interp`, keep it guest-deliverable next to the existing manifest artifact, and preserve bounded host-side translation proof
  - PR4B `startup audit plumbing`: add the minimal Linux/OpenSBI startup-only consumer path so startup segment facts are handed to OpenSBI, expected runtime ranges are reconstructed from manifest segments plus PR3 runtime bases, and audit-only logs are emitted before first user entry
  - PR4C `validation`: preserve one positive matched proof and one bounded mismatch proof using an intentionally wrong startup table, while confirming there is still no enforcement or leaf-tag behavior change
- PR4 coder implementation is now ready for review:
  - top-level commit `a121ed272e6d5bb4eee6e281c167ec54e3dd5dde` (`[CODE]: manifest add startup table tool`) adds `scripts/generate_startup_table.py` and extends `scripts/install_manifest.py` with an optional `--startup-table` install path for `/etc/nacc/startup_table.bin`
  - Linux commit `8ef5a7877f1ed976395e8f82fdbccc8dfb80b4c4` (`[CODE]: riscv audit startup manifest table`) adds the fixed startup-table reader in `linux/arch/riscv/mm/nacc.c`, wires a new `SBI_EXT_NACC_STARTUP_AUDIT_RANGE` call after PR3 coordinate reporting, and keeps `nacc.manifest_mode=enforce` audit-only with an explicit “no enforcement yet” note
  - OpenSBI commit `4d1f679502b4f29083c3faa0d77b8e3bba772ab4` (`[CODE]: sm audit startup manifest ranges`) adds the new startup-audit ecall path plus coverage-only audit logs that reconstruct expected runtime ranges from the stored PR3 runtime bases and the translated startup-table offsets, without changing `nacc_region_select_leaf_tag`
  - route chosen: PR4A translates manifest `PT_LOAD` facts into page-aligned relative offsets/sizes in `startup_table.bin`; PR4B has Linux read `/etc/nacc/startup_table.bin` on the existing startup transitions and forward only role/segment facts; OpenSBI reconstructs expected ranges and audits them against the active root range database; PR4C preserves a host-side correct table plus an intentionally wrong copy that shifts the first record by one page for bounded mismatch proof
- Bounded coder validation for PR4:
  - `python3 -m py_compile scripts/generate_startup_table.py scripts/install_manifest.py` plus diff hygiene across the top-level, `linux/`, and `opensbi/` repos passed; preserved log: `logs/coder/TASK_20260421_004333_manifest_mvp_pr4_sanity_20260422_211614.log`
  - host-side PR4A/PR4C translation proof on the already preserved PR2 manifest artifact wrote `logs/coder/TASK_20260421_004333_manifest_mvp_pr4a_startup_table_20260422_211328.bin` and `logs/coder/TASK_20260421_004333_manifest_mvp_pr4c_startup_table_mismatch_20260422_211328.bin`, with summary log `logs/coder/TASK_20260421_004333_manifest_mvp_pr4a_startup_table_20260422_211328.log`
  - no `make opensbi`, `make linux-update`, or VM/QEMU runtime proof was rerun in this coder turn; those heavier compile/runtime checks remain deferred to reviewer/test_runner per the packet workflow
- Reviewer approved the PR4 code slice for bounded test handoff:
  - direct review confirmed that the packet's frozen PR4A/PR4B route is what landed: `scripts/generate_startup_table.py` emits only entry/interp startup records, `scripts/install_manifest.py --startup-table` keeps the existing PR2 delivery flow, Linux reads only `/etc/nacc/startup_table.bin` after PR3 coordinate reporting, and OpenSBI performs coverage-only audit logging without touching `nacc_region_select_leaf_tag`
  - direct reviewer checks reproduced `python3 -m py_compile scripts/generate_startup_table.py scripts/install_manifest.py`, regenerated the startup table from `logs/coder/TASK_20260421_004333_manifest_mvp_pr2_manifest_source_20260421_134432.json`, and matched the coder artifact hash `b0bf8ac500bf6b26b0ff78c5f56829c3d2e21b0758a2ac8a1d4aff941bfeb5a5`
  - the PR4 matched-runtime proof target is now explicit for test handoff: use the manifest-encoded pair `entry=/tmp/nacc_manifest_smoke_20260421_111813.elf` and `interp=/home/link/NaCC/riscv-tools/sysroot/lib/ld-linux-riscv64-lp64d.so.1`; do not silently substitute the earlier `busybox echo test` workload for the matched run
- Test runner executed the renewed PR4 T1 rerun on `2026-04-22` and stopped at the first fresh runner-owned artifact during exact-byte staging:
  - `python3 -m py_compile scripts/generate_startup_table.py scripts/install_manifest.py` passed; runner log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_sanity_20260422_224141.log`
  - `make opensbi` passed; runner log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_make_opensbi_20260422_224141.log`
  - `make linux-update` passed; runner log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_make_linux_update_20260422_224141.log`
  - positive manifest/startup-table install passed; runner log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_install_positive_20260422_224141.log`
  - the exact-byte `qemu-nbd` staging step stopped before any guest overwrite completed; runner log `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_stage_exact_20260422_224141.log` records the first failure artifact during the mounted-guest hash step with `sudo: a password is required`
  - no `nacc.manifest_mode=audit` QEMU launch, positive audit run, mismatch-table install, or mismatch audit run occurred in this turn
  - runner cleanup log `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_cleanup_after_stage_fail_20260422_224141.log` shows `rootfs` unmounted again and `/dev/nbd0` detached after the stop
- Test runner retried the same PR4 T1 packet on `2026-04-22` after `sudo -n` access was repaired and reached the first runtime failure artifact:
  - no packet-owned rebuilds were rerun on this pass; `linux/` and `opensbi/` stayed unchanged relative to the passing build set already recorded at `20260422_224141`
  - positive manifest/startup-table reinstall passed; runner log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_install_positive_20260422_230329.log`
  - exact-byte staging of the manifest-resolved `entry` and `interp` passed, including guest interpreter backup at `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_guest_interp_backup_20260422_230329.bin`; runner log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_stage_exact_20260422_230329.log`
  - the positive audit-mode boot reached a fresh QEMU runtime artifact but never reached guest auto-run; runner wrapper log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_positive_run_20260422_230329.log`
  - runner-owned logger artifacts for that positive run are `logs/TASK_20260421_004333_manifest_mvp_pr4_positive_qemu_20260422_231111.log` and `logs/TASK_20260421_004333_manifest_mvp_pr4_positive_vm_20260422_231111.log`
  - per the packet stop condition, no mismatch-table install or mismatch audit-mode rerun was attempted after the positive run failed before guest auto-run
  - runner restored the positive startup table plus the backed-up guest interpreter and removed the staged entry afterward; restore logs: `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_restore_table_20260422_230329.log` and `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_restore_guest_20260422_230329.log`
- Log analysis reduced that positive PR4 failure to a pre-auto-run guest userspace crash rather than a manifest-audit mismatch:
  - exact-byte staging replaced guest `/lib/ld-linux-riscv64-lp64d.so.1` hash `d5b56cc6d53b9a405df58e60c3ac4f16c4838e210b607e471a3085c2bb95d20b` with the manifest-resolved hash `20fa0052efa86d73d6896d4983b4f197f13ad6fcc08139f472d0a97c036b04ef`, while the staged entry ELF hash matched host and guest (`logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_stage_exact_20260422_230329.log`)
  - the positive QEMU log reaches `Run /sbin/init as init process`, then `init[1]` takes `unhandled signal 11` in `libc.so.6`, followed by `Kernel panic - not syncing: Attempted to kill init!` (`logs/TASK_20260421_004333_manifest_mvp_pr4_positive_qemu_20260422_231111.log`)
  - no `manifest startup coord` or `manifest startup audit` lines appear in that positive QEMU log before the panic; only the cmdline-level `nacc.manifest_mode=audit` scaffold line appears
- This PR4 run is failed, not merely suspicious: the system never reaches SSH readiness or guest auto-run, so the packet correctly stopped before the mismatch-table rerun and restored the guest image.
- Planner has now converted that runtime phenomenon into a concrete PR4 validation-route decision:
  - the invalidated piece is the guest-global interpreter-swap proof route, not the accepted PR4 audit code slice
  - PR4C is recut as three bounded follow-ups:
    - PR4C1 `probe-root helper`: add a validation-only top-level helper or narrow installer extension that stages the exact manifest `entry` + `interp` pair into a disposable guest-local probe root while leaving the guest-global interpreter untouched
    - PR4C2 `positive audit rerun`: rerun audit mode using that probe root and a root-relative exec path that preserves kernel `PT_INTERP` resolution against the exact staged interpreter; accept only runner-owned startup-audit evidence, not merely guest process exit
    - PR4C3 `mismatch audit rerun`: repeat the same probe-root route with the one-page-shift mismatch table only after PR4C2 reaches audit evidence or yields a new first artifact
  - direct manual `ld-linux ... entry` invocation is now explicitly disallowed for PR4 proof because it bypasses the kernel exec path and would not validate the intended `entry` / `interp` coordinate model
  - if the probe-root route still fails before audit evidence appears or requires additional staged DSOs beyond the fixed pair, the turn must stop again at that first artifact and return to planner instead of widening the bundle implicitly
- PR4C1 coder implementation is now ready for review:
  - top-level commit `97093fa89ee13783970f7026c64dc9a9ca77235d` (`[CODE]: manifest stage probe root`) extends `scripts/install_manifest.py` with optional `--probe-root`, `--probe-entry`, and `--probe-interp` arguments so the existing repo-owned pre-boot `qemu-nbd` + mounted-`rootfs` path can stage the fixed manifest `entry` + exact manifest-resolved `interp` into a disposable guest-local probe root without reinstalling a manifest in the same call
  - route chosen: the helper stages the entry at `/tmp/<entry basename>` inside the probe root, stages the interpreter at `/lib/ld-linux-riscv64-lp64d.so.1` inside the probe root, prints the later root-relative launch form `chroot <probe_root> /tmp/<entry basename>`, and snapshots the guest-global interpreter hash before/after staging so the helper fails closed if that global file changes
  - bounded local proof is preserved in `logs/coder/TASK_20260421_004333_manifest_mvp_pr4c1_sanity_20260422_235036.log` and `logs/coder/TASK_20260421_004333_manifest_mvp_pr4c1_probe_stage_20260422_234927.log`, which show `python3 -m py_compile`, `git diff --check`, detached cleanup, probe-root layout creation at `/tmp/nacc_manifest_probe_root/...`, and unchanged guest-global interpreter hash `d5b56cc6d53b9a405df58e60c3ac4f16c4838e210b607e471a3085c2bb95d20b`
- Reviewer approved the PR4C1 probe-root helper for runner-owned PR4C2:
  - direct review confirmed commit `97093fa89ee13783970f7026c64dc9a9ca77235d` stays entirely inside `scripts/install_manifest.py`, stages only the fixed manifest `entry` + exact manifest-resolved `interp`, preserves the guest-global interpreter, and prints the expected `chroot <probe_root> /tmp/<entry basename>` launch form for the later proof
  - direct reviewer checks reproduced `python3 -m py_compile scripts/install_manifest.py`, `git diff --check 97093fa89ee13783970f7026c64dc9a9ca77235d^ 97093fa89ee13783970f7026c64dc9a9ca77235d -- scripts/install_manifest.py`, and matched the helper's printed `probe_chroot_exec` route against the packet requirement plus the repo's existing `Makefile:237` `chroot` control path
  - no PR4C1 fidelity blocker was found; the remaining uncertainty is still runner-owned runtime evidence about whether the fixed pair reaches startup-audit logs through the probe root without any extra staged DSOs
- Test runner executed the PR4C2/PR4C3 T1 probe-root validation on `2026-04-23` and preserved a fresh runner-owned artifact set:
  - no packet-owned `linux/` or `opensbi/` rebuild was rerun; the active build baseline remained `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_make_opensbi_20260422_224141.log` and `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_make_linux_update_20260422_224141.log`, while `python3 -m py_compile scripts/install_manifest.py` passed again in `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_sanity_20260423_000122.log`
  - positive manifest/startup-table reinstall passed in `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_install_positive_20260423_000122.log`, and exact probe-root staging of the fixed `entry` + exact manifest-resolved `interp` passed in `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_stage_probe_20260423_000122.log` with preserved `probe_chroot_exec=chroot /tmp/nacc_manifest_probe_root /tmp/nacc_manifest_smoke_20260421_111813.elf`
  - the positive one-off audit-mode QEMU launch auto-ran the required `chroot /tmp/nacc_manifest_probe_root /tmp/nacc_manifest_smoke_20260421_111813.elf` path and preserved runner-owned VM/QEMU artifacts `logs/TASK_20260421_004333_manifest_mvp_pr4_probe_positive_20260423_000122_vm_20260423_001011.log` and `logs/TASK_20260421_004333_manifest_mvp_pr4_probe_positive_20260423_000122_qemu_20260423_001011.log`, but direct runner counts on the saved QEMU artifact found `coord=0` and `audit=0`
  - a runner wrapper count-extraction bug allowed the mismatch-table rerun to continue after that zero-count positive pass; mismatch reinstall passed in `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_install_mismatch_20260423_000122.log`, the mismatch one-off audit-mode QEMU rerun preserved `logs/TASK_20260421_004333_manifest_mvp_pr4_probe_mismatch_20260423_000122_vm_20260423_001525.log` and `logs/TASK_20260421_004333_manifest_mvp_pr4_probe_mismatch_20260423_000122_qemu_20260423_001525.log`, and direct runner counts again found `coord=0` and `audit=0`
  - runner restored the positive startup table in `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_restore_table_20260423_000122.log`, removed the staged probe root in `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_cleanup_probe_20260423_000122.log`, and finished with no live repo-owned `qemu-system-riscv64`, no mounted `rootfs`, and only detached `/dev/nbd0`
- Log analysis reduced the runner-owned PR4C2/PR4C3 probe-root artifacts and classified the current fixed-pair route as failed before audit evidence:
  - both positive and mismatch boots reached authenticated SSH readiness and auto-started `chroot /tmp/nacc_manifest_probe_root /tmp/nacc_manifest_smoke_20260421_111813.elf`
  - both VM logs stop immediately after the SSH host-key warning, with no guest-visible stderr and no `[NaCC][ssh-auto-exit]`
  - both QEMU logs boot cleanly to the login prompt but emit no `manifest startup coord` or `manifest startup audit` lines, so the mismatch table never becomes observable as an audit mismatch
  - current-run stage evidence shows only the fixed `entry` + `interp` were staged into the probe root; separate repo-state `readelf -d -V /tmp/nacc_manifest_smoke_20260421_111813.elf` still shows `NEEDED libc.so.6`
  - per the packet stop condition, this now returns to planner instead of widening the staged runtime bundle implicitly
- Planner checkpoint on `2026-04-23`:
  - the pair-only probe-root route is now considered exhausted as a sufficient positive proof path
  - two conditional routes remain explicit rather than silently chosen:
    - park PR4 here and treat the current audit implementation as landed but not yet validated end-to-end on the fixed workload
    - or, if the human explicitly approves continuation, allow one more validation-only slice that stages the exact transitive runtime closure required by the fixed workload into the disposable probe root, while still auditing only manifest roles `entry` and `interp`
  - in either case, the mismatch-table rerun is no longer meaningful until a positive run first emits startup coord/audit evidence
- Planner-local exploratory note on `2026-04-23`:
  - a planner-owned standard-path smoke session was started briefly to check the ordinary container path, but the human redirected this check to test_runner before using it as decision evidence
  - that local session stopped at the host-side authenticated-SSH gate with `[NaCC][ssh-ready-timeout]` before any `docker run` output, and the planner killed the session afterward
  - treat that local attempt as non-authoritative context only; the next actionable evidence must be runner-owned
- Human direction on `2026-04-23`:
  - do not spend planner time rerunning local smoke checks
  - before deciding whether PR4 needs a wider validation-only closure-staging slice, hand one plain non-PR4 baseline container smoke to test_runner to verify that the ordinary system path is still sound
  - this pre-check should stay on the standard path and should not carry any PR4-specific validation machinery
- Test runner executed the requested plain non-PR4 baseline smoke on `2026-04-23` and recorded a passing runner-owned artifact set:
  - runner rebuilt `agent` only because `git -C agent status --short` reported `M Makefile`; build log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_baseline_agent_update_20260423_010933.log`
  - a fresh detached standard-path `make debug VM_AUTO_CMD='docker run --security-opt seccomp=unconfined --rm busybox echo test'` preserved primary runner log `logs/test_runner/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933.log` plus VM/QEMU artifacts `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_vm_20260423_011256.log` and `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_qemu_20260423_011256.log`
  - the VM artifact shows authenticated SSH readiness, `[NaCC] Auto-running: docker run --security-opt seccomp=unconfined --rm busybox echo test`, and guest-visible `test`
  - the QEMU artifact shows `manifest scaffold mode=off`, three startup-coordinate lines, and zero `manifest startup audit` lines
  - cleanup removed the dedicated baseline tmux session and left no live repo-owned `qemu-system-riscv64` owner of `NaCC.qcow2`; forwarded ports `2222` and `1234` were clear after capture
- This runner-owned baseline pass favors “PR4 validation-path-specific problem” over “general container regression” and returns the packet to planner/human for the already required PR4 route decision.
- Planner update on `2026-04-23` after packet review:
  - the packet remains blocked on a human approval gate, not on missing technical intent: the unresolved choice is still whether to park PR4 here or approve one more validation-only slice
  - the planner recommendation remains conditional rather than silently selected: if continuation is approved, name the next slice `PR4C4 closure-staged matched rerun` and keep it top-level plus validation-only
  - `PR4C4` must widen only the disposable probe root, not the audited startup object set: derive the exact transitive runtime closure for the fixed manifest `entry` under the exact manifest-resolved interpreter/toolchain/sysroot, stage those exact bytes into `/tmp/nacc_manifest_probe_root`, but continue to audit only manifest roles `entry` and `interp`
  - the first `PR4C4` attempt should leave Linux/OpenSBI/agent unchanged, preserve a readable closure artifact list, rerun only the positive matched case first, and stop immediately if startup coord/audit evidence still does not appear
  - if the human does not approve `PR4C4`, park PR4 explicitly as `code accepted, matched end-to-end validation unresolved on the fixed workload`; do not blur that validation gap into PR5
- Human direction on `2026-04-23` after the baseline pass:
  - the latest failure pattern may mean the current host-side generated proof ELF is itself the wrong validation anchor, rather than merely a host-side closure-staging omission
  - the human is therefore considering a pivot of the next validation focus toward the guest area instead of continuing to invest only in host-generated ELF proof machinery
  - this is not yet a frozen route choice: planner should keep it explicit as an alternative to `PR4C4`, not silently replace the current conditional host-side closure route with a guest-side redesign in the same turn
- Human clarification on `2026-04-23` after route discussion:
  - the x86-host / RISC-V-target split is not, by itself, treated as suspicious or architecturally wrong for this project; the real deployment environment is still RISC-V, and cross-building or host-side preparation for a RISC-V target is expected rather than disqualifying
  - the stronger concern is semantic alignment, not build-host purity: later validation should be centered on the RISC-V-side runtime environment the confidential container actually sees
  - therefore a guest-area pivot is only one possible consequence, not an automatic requirement; host-side generation remains acceptable if the proof target, loader path, and closure all correspond to the intended RISC-V guest/runtime reality rather than an arbitrary convenience ELF
- Human archival decision on `2026-04-23`:
  - this packet is intentionally being shelved and archived rather than pushed through the remaining PR4 route decision
  - the reason is packet overload and intent drift, not a new technical rejection of the accepted PR0-PR4 code history
  - do not ACK or continue machine-to-machine from this packet; if the work resumes, start from a fresh condensed test packet instead of reopening this one in place
  - the fresh packet should carry forward only the durable context that still matters: PR0-PR3 landed history, accepted PR4 audit-code boundary, the ordinary-path baseline pass, and the unresolved need for a RISC-V-runtime-aligned validation anchor

## Next Handoff

- Next owner: human
- Trigger: the human chose to shelve this overloaded packet and archive it without further ACK or continuation.
- Exact artifact to read first: `docs/workflow/tasks/completed/TASK_20260421_004333_manifest_mvp.md` once archived, then `logs/test_runner/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933.log`, `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_vm_20260423_011256.log`, and `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_qemu_20260423_011256.log`.
- Exact task for next owner: if and when this work resumes, seed a fresh condensed test packet instead of reopening this one. That new packet should explicitly decide:
  - which accepted history survives unchanged from this archived packet
  - whether PR4 validation restarts from a RISC-V-runtime-aligned proof target
  - which old PR4 validation routes are discarded rather than re-litigated
- Expected deliverable: archive this packet now; later, a separate fresh test packet with a smaller scope and explicit Goal / Constraints / Definition Of Done.
- Stop condition: stop after archival. Do not ACK, do not continue the current role chain, and do not open a replacement packet in this turn.
- Do not do in this turn: do not rerun baseline or PR4 validation, do not make a new machine-owner handoff, do not silently carry this packet forward as the live source of truth, and do not start a replacement packet until the human manually initiates it.

## Coder Result

- Implementation summary:
  - Top-level commit `97093fa89ee13783970f7026c64dc9a9ca77235d` (`[CODE]: manifest stage probe root`) extends `scripts/install_manifest.py` so the existing repo-owned pre-boot `qemu-nbd` + mounted-`rootfs` flow can stage the fixed manifest `entry` + exact manifest-resolved `interp` into a disposable guest-local probe root without reinstalling a manifest in the same call.
  - The helper now accepts optional `--probe-root`, `--probe-entry`, and `--probe-interp` arguments, stages the entry at `/tmp/<entry basename>` inside the probe root, stages the interpreter at `/lib/ld-linux-riscv64-lp64d.so.1` inside the probe root, and prints the later root-relative launch form `chroot <probe_root> /tmp/<entry basename>`.
  - The same helper snapshots the guest-global interpreter hash before/after probe staging and fails closed if the guest-global `/lib/ld-linux-riscv64-lp64d.so.1` changes, so the invalidated global-replacement route cannot silently reappear through this installer path.
- Commit or patch:
  - top-level commit `97093fa89ee13783970f7026c64dc9a9ca77235d` `[CODE]: manifest stage probe root`
  - packet/report updates for reviewer handoff remain in the top-level worktree for this turn and are not part of the top-level code commit above
- Route chosen and why:
  - Chose a narrow extension to `scripts/install_manifest.py` instead of a sibling helper so PR4C1 reuses the same repo-owned `qemu-nbd` + mounted-`rootfs` mutation path that PR2 and PR4A already validated, rather than inventing a second qcow2 write route just for the probe-root experiment.
  - Kept the PR4C1 layout intentionally pair-only: the helper stages only the fixed `entry` and exact manifest-resolved `interp`, with no libc or broader runtime bundle, because the packet explicitly requires stopping for a new planner checkpoint rather than widening to extra staged DSOs implicitly.
  - Made the invalidated route fail closed in the helper itself by checking the guest-global interpreter hash before/after staging. This keeps the new proof route explicit: the later runner proof must use the printed `chroot <probe_root> /tmp/<entry basename>` form inside the disposable probe root, not a silent guest-global overwrite.
- Escalations made:
  - none
- Minimal validation:
  - `python3 -m py_compile scripts/install_manifest.py` passed; preserved with `git diff --check -- scripts/install_manifest.py` and detached cleanup notes in `logs/coder/TASK_20260421_004333_manifest_mvp_pr4c1_sanity_20260422_235036.log`
  - bounded live pre-boot staging passed with `python3 scripts/install_manifest.py --probe-root /tmp/nacc_manifest_probe_root --probe-entry /tmp/nacc_manifest_smoke_20260421_111813.elf --probe-interp /home/link/NaCC/riscv-tools/sysroot/lib/ld-linux-riscv64-lp64d.so.1`; preserved log: `logs/coder/TASK_20260421_004333_manifest_mvp_pr4c1_probe_stage_20260422_234927.log`
  - the stage log records the intended guest probe-root layout, the printed later `probe_chroot_exec=chroot /tmp/nacc_manifest_probe_root /tmp/nacc_manifest_smoke_20260421_111813.elf` launch form, and unchanged guest-global interpreter hash `d5b56cc6d53b9a405df58e60c3ac4f16c4838e210b607e471a3085c2bb95d20b`
- Remaining risks:
  - whether the fixed `entry` + `interp` pair can actually reach PR4 audit evidence through the printed `chroot` route without staging any extra DSOs remains unproven; the packet still requires reviewer/test_runner to stop at the first such artifact rather than widening the bundle implicitly
  - the helper defaults the staged entry path inside the probe root to `/tmp/<entry basename>`; for the current fixed pair that preserves `/tmp/nacc_manifest_smoke_20260421_111813.elf`, but reviewer should call out any semantic mismatch instead of silently assuming that default generalizes beyond this PR4C route
  - this turn did not run VM/QEMU proof or the PR4C2 positive audit rerun, so runtime evidence for the probe-root route remains deferred to reviewer/test_runner

## Review Result

- Approval status: approve
- Spec fidelity: approved. The PR4C1 change stays within the packet's narrowed validation-only route instead of reopening PR4 architecture or widening the staged bundle. `scripts/install_manifest.py:39-82` adds only probe-root arguments to the already accepted top-level installer, and `scripts/install_manifest.py:169-194` constructs a probe spec that keeps the staged entry at `/tmp/<entry basename>` and the staged interpreter at `/lib/ld-linux-riscv64-lp64d.so.1` inside that probe root. `scripts/install_manifest.py:303-330` stages exactly two probe artifacts and nothing beyond the fixed manifest `entry` + exact manifest-resolved `interp`. `scripts/install_manifest.py:360-385` snapshots the guest-global interpreter hash before probe staging, clears only the selected probe root, and fails closed if the guest-global `/lib/ld-linux-riscv64-lp64d.so.1` changes, so the invalidated guest-global replacement route cannot silently reappear. `scripts/install_manifest.py:418-427` prints `probe_chroot_exec=chroot <probe_root> /tmp/<entry basename>`, which matches the packet's required kernel `PT_INTERP` proof model and the repo's existing `Makefile:237` `chroot` control path. I found no Linux/OpenSBI edits, no extra DSO staging, no manual `ld-linux ... entry` fallback, and no startup-policy or leaf-tag changes in this slice.
- Risk review: acceptable for T1 handoff. The remaining risks are runtime-proof risks, not fidelity blockers. The fixed `entry` + `interp` pair may still fail before any audit evidence if the staged workload needs additional guest-local DSOs; the packet already requires test_runner to stop at that first artifact and return to planner instead of widening the bundle. The helper also accepts broad absolute `--probe-root` values and clears that directory inside the mounted guest image before staging (`scripts/install_manifest.py:140-151,360-364`), so the approved route is narrower than the raw interface: runner should stay on the packet-owned disposable path `/tmp/nacc_manifest_probe_root` for PR4C2/PR4C3 and should not treat arbitrary guest locations as part of the reviewed contract.
- Can proceed to test: yes
- Key files reviewed:
  - `docs/workflow/tasks/active/TASK_20260421_004333_manifest_mvp.md`
  - `docs/workflow/CURRENT_STATE.md`
  - `scripts/install_manifest.py`
  - `logs/coder/TASK_20260421_004333_manifest_mvp_pr4c1_probe_stage_20260422_234927.log`
  - `logs/coder/TASK_20260421_004333_manifest_mvp_pr4c1_sanity_20260422_235036.log`
  - `logs/test_runner/TASK_20260421_004333_manifest_mvp_pr4_stage_exact_20260422_230329.log`
  - `logs/TASK_20260421_004333_manifest_mvp_pr4_positive_qemu_20260422_231111.log`
  - `Makefile`
- Direct reviewer checks:
  - `python3 -m py_compile scripts/install_manifest.py`
  - `git diff --check 97093fa89ee13783970f7026c64dc9a9ca77235d^ 97093fa89ee13783970f7026c64dc9a9ca77235d -- scripts/install_manifest.py`
  - `rg -n "chroot|startup_table.bin|manifest.json|probe_root" Makefile scripts/install_manifest.py docs/workflow/tasks/active/TASK_20260421_004333_manifest_mvp.md`
- Short human-facing code explanation: this follow-up teaches the existing qcow2 installer to stage the exact audited program and loader into a disposable probe root inside the guest image, print the later `chroot` exec form, and fail closed if the guest's real `/lib/ld-linux-riscv64-lp64d.so.1` changes.
- Why the route still fits: it reuses the already accepted pre-boot qcow2 mutation path, stays entirely top-level and validation-only, preserves the kernel `PT_INTERP` proof model via `chroot`, and replaces the invalidated guest-global interpreter-swap route without widening to a broader runtime bundle or Linux/OpenSBI change.
- Human-facing summary: reviewer approved the PR4C1 probe-root helper for runner-owned PR4C2. The next test should use the packet-owned `/tmp/nacc_manifest_probe_root` route under `nacc.manifest_mode=audit` and stop immediately if the fixed pair still needs extra DSOs before any startup-audit evidence appears.

## Test Result

- Command run:
  - `make agent-update`
  - `make debug VM_AUTO_CMD='docker run --security-opt seccomp=unconfined --rm busybox echo test'`
  - `make logger LOG=TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933`
- Build actions:
  - runner rebuilt `agent` only because `git -C agent status --short` reported `M Makefile`; build log: `logs/test_runner/TASK_20260421_004333_manifest_mvp_baseline_agent_update_20260423_010933.log`
  - no packet-owned `linux/` or `opensbi/` rebuilds were rerun for this bounded standard-path baseline smoke
  - runner preserved the primary wrapper log plus VM/QEMU artifacts at `logs/test_runner/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933.log`, `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_vm_20260423_011256.log`, and `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_qemu_20260423_011256.log`
- Outcome:
  - passed
- Primary log path:
  - `logs/test_runner/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933.log`
- Artifact / log path:
  - `logs/test_runner/TASK_20260421_004333_manifest_mvp_baseline_agent_update_20260423_010933.log`
  - `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_vm_20260423_011256.log`
  - `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_qemu_20260423_011256.log`
- Cleanup state:
  - stale session `tr_baseline_20260423_010824` and one repo-owned QEMU holder of `NaCC.qcow2` were cleared before the fresh launch
  - the dedicated baseline session `tr_baseline_20260423_010933` was killed after logger capture
  - direct post-run check found no live repo-owned `qemu-system-riscv64` owner of `NaCC.qcow2`; forwarded ports `2222` and `1234` were clear after cleanup

## Analysis Result

- Post-run analysis required:
  - no
- Verdict: passed; the ordinary non-PR4 standard path still reaches authenticated SSH readiness, auto-runs the baseline container command, and prints guest-visible `test`
- Dominant event pattern:
  - the fresh standard-path baseline boot reached `[NaCC][ssh-ready]`, auto-ran `docker run --security-opt seccomp=unconfined --rm busybox echo test`, and preserved guest-visible `test` in the VM artifact
  - the paired QEMU artifact stayed on `manifest scaffold mode=off`, recorded three startup-coordinate lines, and recorded zero `manifest startup audit` lines
- Run acceptability:
  - passed
- First decisive point:
  - `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_vm_20260423_011256.log` contains the baseline auto-run line and guest-visible `test`; the paired QEMU artifact `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_qemu_20260423_011256.log` records `manifest scaffold mode=off` with zero `manifest startup audit` lines
- Human-facing summary: The ordinary container baseline still works on the current standard path. This runner-owned pass favors “PR4 validation-path-specific problem” over “general container regression.”
- Evidence:
  - `logs/test_runner/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933.log`
  - `logs/test_runner/TASK_20260421_004333_manifest_mvp_baseline_agent_update_20260423_010933.log`
  - `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_vm_20260423_011256.log`
  - `logs/TASK_20260421_004333_manifest_mvp_baseline_smoke_20260423_010933_qemu_20260423_011256.log`
- Confidence:
  - high
- Evidence / Inference Boundary: Observed runner-owned facts for this turn are limited to the baseline smoke: `make agent-update` passed, the VM artifact shows authenticated SSH readiness plus guest-visible `test`, and the paired QEMU artifact shows `manifest scaffold mode=off` with zero `manifest startup audit` lines. The statement that this favors “PR4 validation-path-specific problem” over “general container regression” is a routing inference from those saved baseline artifacts plus the already recorded failed PR4 probe-root artifacts above; this turn does not add new probe-root diagnosis.
- Recommended next owner: human
- Recommended next step: decide whether to park PR4 here, approve `PR4C4 closure-staged matched rerun` under the current host-side limits, or freeze a guest-area pivot as the next planning step before any coder handoff.

## Open Questions

- For PR5 MVP, if real RO enforcement is not already available on the chosen path, is conservative `MEASURED_RO -> PRIVATE_DATA` the accepted first landing?
- For PR4 validation, should PR4 now be parked with the current audit code accepted but matched validation unresolved, or is `PR4C4 closure-staged matched rerun` explicitly approved?
- If that wider PR4 validation slice is approved, must every extra staged DSO come from the same manifest-resolved host/toolchain closure with no guest-native substitution, and must the rerun order remain positive-first before any mismatch-table repetition?
- If host-side proof widening is rejected, what exactly should the guest-area pivot freeze as the next validation anchor: a guest-resident ELF already present in the image, a guest-rootfs-derived manifest/workload pair, or a later registration-path manifest delivery route?
