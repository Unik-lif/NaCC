# Human Progress Report

- Task ID: TASK_20260419_115218_private_baseline
- Task Packet: `docs/workflow/tasks/active/TASK_20260419_115218_private_baseline.md`
- Created: 2026-04-19 11:52:18 +0800
- Purpose: cumulative human-readable updates appended by coder, reviewer, and log_analyzer
- Append Rule: add a new timestamped section for each new turn; do not rewrite older entries
- Reading Hint: newest updates are appended at the end of this file

## 2026-04-19 14:06:49 +0800 - coder

Implemented the Phase-1 fail-closed route. Ordinary candidate leaves now resolve to `PRIVATE_DATA` even for lookup misses, `PRIVATE_FILE_COW`, `SHARED_EXPLICIT`, ambiguous `SPECIAL_EXCLUDED`, and missing region state under a tagged `ROOT_L0`. Linux-side sync stayed attribution-only. I also extended the final `PRIVATE_DATA` dump so it reports bounded path-category counters and richer hot-`mepc` samples, and I added bounded Linux provenance logs plus the focused `config/debug-batch.private_baseline_t1.txt` batch file.

What stayed intentionally unchanged: the `ROOT_L0` lifecycle, the ordinary-leaf candidate gate, Linux sync ABI shape, and the ownership of runtime validation by reviewer/test_runner. Minimal validation at this point was compile-only: `make opensbi` and `make linux-update`.

## 2026-04-19 14:11:48 +0800 - reviewer

Review verdict was approve-with-conditions. The route was faithful: enforcement authority stayed in OpenSBI reconcile/install, Linux sync remained hint-only, and the change did not widen policy scope or invent a new metadata system. The main risk was attribution quality, not enforcement correctness. The final hot-bucket output still looked too likely to collapse broad helper traffic into stale kernel-side samples, so the first runtime batch needed to prove the final report was actually legible enough for follow-on optimization decisions.

## 2026-04-19 16:41:34 +0800 - log_analyzer

The first packet-owned run family (`155129`) was successful but still suspicious. All probes completed, but the final output was too opaque: the top-level view was dominated by `syscall_buffer_path` plus `teardown_mapping_update`, and hot `mepc[]` rows in the file/pipeline probes still collapsed to kernel PCs with invalid ranges. There was enough evidence to say enforcement was active, but not enough to say the final report preserved the real hot file/COW/shared sources. That result correctly routed the packet back to coder for a reporting-only follow-up.

## 2026-04-19 16:49:45 +0800 - coder

Implemented a narrow reporting-only follow-up in `opensbi/lib/sbi/sbi_trap_ldst.c`. The final dump now emits bounded `provenance[]` rows keyed by `root_pfn + range + class + flags`, with per-bucket `anon/file/cow/unknown` counters, and hot-`mepc` buckets can refresh their sample when a later hit has better user-side attribution. Enforcement stayed unchanged. Minimal validation here was compile-only: `make opensbi`.

## 2026-04-19 16:58:58 +0800 - reviewer

The reporting follow-up stayed packet-faithful and sufficiently narrow. Review again approved the route with conditions: rerun the packet-owned T1 batch and confirm that the new `provenance[]` surface keeps file/pipeline, fork/COW, and shared-memory probes joinable by `root + range` even when kernel helper PCs dominate the top `mepc[]` rows.

## 2026-04-19 19:19:29 +0800 - log_analyzer

Authoritative final verdict: acceptable. The accepted rerun family is `181934`, not the earlier `155129` family. All eight runs completed successfully, the dominant end-of-run pattern stayed helper-heavy (`syscall_buffer_path` first, `teardown_mapping_update` second for runs 1 through 7; teardown-heavy shared-memory cleanup in run 8), and the final output is now decision-grade because hot file/COW/shared sources are joinable through `provenance[]` plus Linux provenance lines.

Representative joins:
- run 4 keeps a file-backed COW range that joins to `/lib/ld-linux-riscv64-lp64d.so.1` and still leaves `/bin/cat` visible nearby
- run 6 preserves a dedicated `file_fault` bucket that joins to `/lib/libc.so.6`
- run 7 keeps a file-backed COW range that joins to `/lib/ld-linux-riscv64-lp64d.so.1` and still leaves `/bin/wc` visible nearby
- run 8 cleanly separates `/nacc_shm_repro` from `/dev/shm/nacc-mini-shm-1`

What this means: the packet is done from a log-analysis perspective. The remaining question is only what to optimize next. If follow-on work is opened, it should be a fresh planner packet around syscall-buffer/helper cost or the joined file-backed/shared hot ranges.

## 2026-04-19 19:21:00 +0800 - log_analyzer

Maintenance note: multiple near-duplicate `log_analyzer` appends were generated by an organizer duplicate-dispatch incident during this run. Those duplicate entries were intentionally collapsed into the authoritative summary above so this report remains readable.
