## 实验记录
先前在qemu上的实现稍微有点问题，已经更正过来了

要让pmp异常能够被opensbi这边处理，而不是转发给linux，需要对opensbi做相关的改动

### opensbi对于页面异常的处理
按照下面的步骤走
- riscv存在对于trap instruction的直接读取，需要分析其二进制表示，来确认是什么类型的指令触发的异常
- 同样需要知道对应的指令，和其对应的指令长度，用来处理完异常后，跳转到正确的PC位置

这一步看起来还需要进行页表遍历，因为出现异常的时候，我们看到的是虚拟地址，Opensbi自己则不通过虚拟地址来进行处理，而是通过物理地址
