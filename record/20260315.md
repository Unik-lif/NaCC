## 2026-03-15

- `VM_NACC` / agent aperture 误继承这条线已明显缓解：Linux 与 OpenSBI 两侧 filter 都已生效。
- 当前 fork+exec 的主问题后移到 child 新 PTP 的 Linux 生命周期闭环，尤其是 ctor / ptlock / dtor 语义。
- 最新最强假设：OpenSBI 为 child 新分配的 secure PTP，Linux 侧虽然拿到了 `ptp_list`，但未必真正补齐到与 `pmd_alloc/pte_alloc` 等价的完成态。
- 下一步先围绕 `ptp_list -> pagetable_*_ctor -> ptdesc->ptl -> pagetable_*_dtor` 加验证点，不再把重点放回 `VM_NACC` 误继承。
